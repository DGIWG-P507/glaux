# Glaux Server review findings and assessment

**Checkpoint:** September 20, 2026, through Pass 3c iteration 40 (queue batch 22, the Phase 3 tasks). All 39 were assessed and **all 39 are adequate**, bringing the total to 125 assessed with none found inadequate. Phase 3 is where observation semantics first meet storage and query, and the tasks reproduce the Guide's hardest rules almost word for word; the `latest`-selection task carries line 440 in full, including the tie retention the peer implementation examined earlier does not honour. **F-19** is confirmed a second time at task level: the Phase 3 restore task, like the Phase 1 one, omits audit records. The coverage counts are now **computed from the issue records** rather than restated as prose, which removes the class of error behind three earlier counting mistakes. The previous checkpoint, iteration 39, withdrew the F-12 tallies and assessed the 31 Phase 2b tasks. A counting claim is withdrawn: the tallies published alongside **F-12** in the two previous iterations conflated not deriving an expected answer from server output with the separate question of whether a runner may decode the actual response through production types. Neither tally counted the second, so no ratio between them meant anything, and **no replacement census is needed**, because a corpus count cannot answer it. F-12 itself is unchanged and rests where it always did, on the Guide-text analysis of line 901. The targeted-only issue count is corrected to 12. All 31 Phase 2b tasks were then assessed and **all 31 are adequate**, bringing the total to 86 assessed with none found inadequate. **F-09**'s owning task carries the guard against the wrong route inference, narrowing but not closing its residual. The previous checkpoint, iteration 38, assessed the 31 Phase 2a tasks. The issue read count published last iteration was wrong: 42 came from adding batch totals without removing issues counted twice. The corrected figure is **37**, derived from distinct issue IDs, and that derivation immediately caught a second overlap in this batch before publication. All 31 Phase 2a tasks were assessed and **all 31 are adequate**, bringing the fully assessed total to 55 with none found inadequate. **F-12** is unchanged on its Guide-text basis; the tallies published alongside it in iterations 37 and 38 conflated two different rules and are withdrawn in iteration 39. A fourth defect in the review's own saved state was corrected before assessing, a batch title claiming a group boundary that does not exist; no coverage gap resulted. The previous checkpoint, iteration 37, assessed the 24 Phase 1 tasks. All 24 Phase 1 tasks were assessed as whole units, scope and acceptance criteria and added fields together, and **all 24 are adequate** against the Guide. **F-12 gained its first quantified support**: six of the 24 state, in their own words, the exact response-interpretation boundary the Guide leaves open, and a corpus-wide search puts it at a lower bound of 24 of 286. The boundary is therefore stated where an author happened to think of it rather than as a rule, which is precisely what the recorded one-clause remedy would fix. The reviewer-read issue count rises from 18 to 42. The previous checkpoint, iteration 36, completed the whole-backlog analysis. The backlog analysis is complete and clean: **1,676 dependency edges across 286 tasks with no cycle, no dangling reference and no dependency running backwards in time**, a single root matching the Roadmap's own starting task, every Guide capability section cited by some task, and coherent sizing. Every mechanical check was proven able to detect an injected defect before its clean result was accepted, and three apparent problems turned out to be artifacts of the analysis rather than defects in the backlog. One correction was made first: the claim recorded last iteration, that the remaining issue batches reduce to the content the issues add beyond their leaves, is **withdrawn**. Matching content is reused as one shared copy and still receives semantic review, because mechanical equality does not establish adequacy. The previous checkpoint, iteration 35, compared all 286 issues against their leaves and found no difference. All 286 implementation issues were compared against their Roadmap leaves and **the difference inventory is empty**, confirmed not by a similarity score but by finding every leaf's scope and acceptance text reproduced verbatim in its issue. Three things made that zero worth trusting: every check was proven able to detect an injected difference, a deliberately wrong pairing was correctly flagged, and one reviewer error in the comparison itself was caught and corrected during the run. The batch also found the third defect in the review's own saved state: the scope never named the repository, and the issues live in the implementation repository rather than the planning one that holds this review. The queue's largest recorded sizing uncertainty resolves favourably, since matching content genuinely need not be reviewed twice. The documented issue read count stays at 18, because fetching a body is not reading it. The previous checkpoint, iteration 34, completed the verification area; five of seven areas are closed. The verification strategy establishes all three things this pass assessed: independent expected answers, meaningful failure detection and real-system checks matched to the claim being made. Every planned capability has a layer that can observe its claims, and no gap was found between what the strategy promises and what its layers can deliver. Two findings were materially refined without changing disposition. **F-12** moves to its real home at Guide line 901 and its residual is narrower and better supported than the review had recorded. **F-09**'s general rule turns out to be adopted already, leaving only fixture content owned by an existing issue. The previous checkpoint, iteration 33, completed the scenarios area and gave four findings named remedy locations. Every Goal capability area is reachable from both a representative scenario and a whole-guide walkthrough path, and no scenario asserts a guarantee the design does not establish. Security and verification are cross-cutting by the Guide's own stated design rather than having dedicated paths, which is recorded as a choice and not a gap. The batch's main product is that **four existing findings gained a named remedy location**: F-03, F-19, F-18 and F-11 each now point at the specific scenario where the recorded remedy would go. No disposition changed, no research report was reopened, and one more reference defect was found and corrected in the review's own saved state before reading. The previous checkpoint, iteration 32, closed the research key-section sweep and completed review area 3. Across all 71 topic reports, 18 are fully read including all four committed deep reads and 53 are screened at their decision-usable sections; no partial read remains and no report is left with unrecorded read depth. Two limits stay on the record: a screen is not a full read, and each report's entry lists exactly what was and was not covered. This batch also found the first **F-17** false positive, a matched line that is not stale because it distinguishes accepting a report from adopting its recommendations. That prompted reading all sixteen stale-wording matches in place: all sixteen are genuine, so the population stays at twenty, now on a verified basis rather than a pattern match. **F-01** gained a fact that removes an apparent blocker without changing the finding. No new finding number and no new follow-up question. The queue stands at **27 batches, 14 done and 13 remaining**, recorded in `batch_queue` of [review-state.json](review-state.json). Every remaining batch covers scenarios, verification, issues and the final assessment.
**Status:** Partial independent review; no implementation changes approved or performed by this checkpoint.
**Purpose:** Preserve paid-for work and let another reviewer continue without chat memory or a restart.

Read this file and [the continuation instructions](instructions.md) first. Consult archived reports only for a specific finding or evidence question. Do not load the entire archive just to resume.

## What is saved, and what is not

- All **15 supplied Copilot responses** are preserved in `evidence/`. Public copies normalize local workstation paths where needed; the remaining responses are byte-identical to the supplied files.
- Original and published byte counts and SHA-256 hashes, plus the exact privacy transformations, are in [the source manifest](evidence/source-manifest.json). Untouched originals were also retained locally. Hashes establish copy integrity, not correctness of the review.
- The superseded long continuation brief is retained as `evidence/16-superseded-resumption-brief.md` for history only. It is **not** active instruction.
- The **22 numbered findings, including two withdrawals**, are consolidated below. Reported severities are preserved; they are not newly validated scores or approvals to implement.
- This file is the authoritative findings record. [The machine-readable checkpoint](review-state.json) tracks all **71 topic-report filenames**, all **286 implementation-issue IDs**, review coverage and the active handoff. It does not duplicate the findings.
- Source-report facts and Codex's subsequent qualifications are distinguished here. Some latest qualifications were not yet incorporated by Copilot when the last supplied response was written.
- These copies are the review reports, not a complete archive of every standard, repository page or issue body the reviewer consulted. Pinned project sources and targeted original evidence may still need to be accessed.
- No private Copilot memory file has been located, read or verified. This handoff does not depend on it.
- The shared home is `Docs/Plans/glaux-server/Review` in the planning repository. Git history records published updates; reviewers must distinguish saved local work from a verified remote commit. Provider memory is not the record.

## Interim assessment from the existing work

The available review has produced specific licensing, repository-control, interpretation, test and accountability concerns. It has **not established a need to discard the research, replace the server architecture or rewrite the entire Roadmap**. It also has **not completed the promised comprehensive validation**.

The earliest recorded action candidates are project-license selection around #4 and required-check enforcement after #6/before subsequent code merges. Later candidates have their own affected task or phase. They should not all become blanket prerequisites to the first foundation task. This is a summary of existing recommendations, not authorization to start coding or a guarantee that unreviewed areas contain no defects.

No defensible overall effort percentage, remaining dollar cost or remaining turn count has been established. An issue-body read count, now 18 of 286 after the iteration 16 bookkeeping correction, is a coverage count and not proof that a matching share of the effort remains. The earlier broad characterization of being less than halfway through the work was not a measured effort estimate.

## Coverage at the checkpoint

| Area | Documented work | Not established complete |
|---|---|---|
| Baseline | Goal v1.8, Guide v1.3, Roadmap v1.18, README, CONTRIBUTING and issue template read | Only later changes would need targeted checking |
| Standards | Substantial pinned-source checks; Part 1 normative body/Annex A read; targeted Part 2 and other checks; SWE Common 3.0 quality and array-flag interpretations verified against 24-014 and the published schemas (iteration 12); Features Part 3/CQL2 class identifiers, dependencies, `cql2-json`-only default and GeometryCollection `minItems` verified against 19-079r2, 21-065r2 and `cql2.json`, and the four SensorML 3.0 JSON class identifiers verified against 23-000 (iteration 13); all thirteen IDR-011 Section 14.3 abstract-test rows confirmed against the published 23-001/23-002 Annex A text and the Part 2 Annex A.1 inheritance chain verified (iteration 14); the F-13 extension-rule question resolved against 23-002 Clause 16.1.5, the tagged and published `observation.json`, 23-001 Clause 19, RFC 7946 Section 6.1 and 23-000 Requirements 9/10 (iteration 15) | None outstanding for the carried checks; later passes may raise targeted standards questions |
| Research | **Complete for the recorded scope.** All 71 topic reports accounted: 18 fully read including all four committed deep reads, 53 screened at their recommendations, risks and open questions, and decision registers. No partial read remains and no report has unrecorded read depth. Peer-source pin checks complete and closed | **Met.** The screen deliberately excluded executive summaries, bodies, appendices and validation-against-plan sections, and each report's entry records what was and was not covered, so a later pass can reopen a specific report without re-deriving coverage |
| Scenarios | Selected transaction, command, security and disconnected-operation analysis | Systematic end-to-end pass |
| Test quality | Relevant research and selected issue assertions examined | Systematic verification-credibility pass |
| Issues | #3, #15, #19, #21, #22, #56, #71, #72, #98, #99, #104, #130, #156, #163, #164, #168, #174, #240 reported fully read | 268 other bodies; complete dependency/coverage/sizing review |
| Final report | This preserved partial assessment | Completed comprehensive assessment |

The original six-pass review is the completion objective, as summarized in [the start page](README.md). Work is performed in bounded, user-authorized iterations. A subscription limit creates a handoff, not a restart or a completed-review claim. Scope changes must be explicit; no reviewer may quietly convert the remaining inventory into unlimited work.

### Remaining checks already named in the review

Standards: none outstanding. (SWE quality and array flags were closed as supported in iteration 12; Features Part 3/CQL2 identifiers with GeometryCollection `minItems` and the SensorML class identifiers were closed as supported in iteration 13; the nine remaining IDR-011 Section 14.3 abstract-test rows were confirmed and the Part 2 Annex A.1 inheritance question was closed as supported in iteration 14; the F-13 extension-rule question was resolved in iteration 15; see "Resolved standards checks" below.)

Research portions: all four partial-report remainder slices are closed. IDR-008 sections 16-19, IDR-037 appendices A-D, IDR-038 appendices A-C and IDR-039 section 7 and sections 21-22 closed as slice 1 in iteration 16; IDR-040 sections 9-21 as slice 2 in iteration 17; IDR-042's unread sections as slice 3 in iteration 18; IDR-043's unread sections as slice 4 in iteration 19. Those seven reports are now fully read. IDR-030 was then completed as queue batch 2 in iteration 20. Remaining research work: three committed deep reads, 034, 039A and 055, none yet begun (queue batches 3-5); the key-section screen of the 47 reports whose read depth was never recorded, together with the unread sections of the six partial reports (queue batches 6-14); and the CS-GO/OSH pinned-source spot checks, merged into queue batch 8.

**The complete remaining queue.** Every remaining finish condition across all six areas is mapped to a finite numbered batch in `batch_queue` of [review-state.json](review-state.json): **27 batches, 2 done and 25 remaining.** The queue separates genuinely unreviewed work from coverage that was simply never recorded, and from bookkeeping already corrected in iteration 16. Iteration 20 corrected two accounting problems in it. Issue fidelity is now established by a complete mechanical comparison of all 286 issues against their Roadmap leaves rather than by an 18-issue sample, because a sample cannot establish complete coverage; feasibility was verified against the live public API before that change was recorded. The six partial reports (IDR-006, 007, 010, 014B, 017 and 044) were omitted from the first version and are now assigned to the cluster batches already covering their topics, reusing their recorded evidence and screening only unread key sections, with no batch added for them. Three issue batches were combined as a consequence of the complete comparison, since matching leaf content is reused rather than reviewed twice, which is what moved the count from 30 to 27. The queue is a workload estimate with six recorded uncertainties, not a quota and not a completion guarantee.

These entries preserve unfinished commitments. The broad research obligation is a key-section sweep plus consequential deep reads, not cover-to-cover rereading of every historical report or plan. Reuse documented work and keep this assessment usable during completion.

## Findings register

**Reading the status:** "Reported" describes the saved review, not a new audit. "Candidate" or "proposed" requires judgment before adoption. "Withdrawn" stays withdrawn absent specific new contrary evidence. No finding below records an implemented fix or project-lead approval of its proposed change.

### F-01 - Project license

**Status:** Reported omission. **Reported severity:** Medium.
**Affected work:** #4; later release checks.

The inspected baseline had no project license; dependency and bundled-artifact notices do not select one. License selection is an owner/organizational decision, not delegated by the review.

**Next consideration:** Confirm/select the project license before the affected dependency/code work; do not select one automatically.

**The apparent blocker is resolved (iteration 32, IDR-062).** The context below could read as a live obstacle, and it is not. IDR-SRV-062 recommendation 5 settles the practical question: "**Verify reuse terms only if direct copying is proposed.** No source/fixture reuse is currently required. Independently implement Rust behavior from authoritative standards and the accepted Glaux design." Its §7.1 adds that peer examples "are not requirements to copy every peer helper, tool or abstraction." So the MPL-2.0 obligation and the absent-licence constraint recorded below do not bite, because no reuse is planned. This finding's own subject, selecting the project's licence, is untouched and its disposition does not change. Evidence: [37-pass-3c-32-batch14-final-research-cluster.md](evidence/37-pass-3c-32-batch14-final-research-cluster.md), §6.

**Context from the peer studies (iteration 26).** Two studies record concrete licensing constraints on reusing peer material, which is why the selection matters in practice rather than only formally. IDR-SRV-014A R-014A-014 directs the project to "review MPL-2.0 obligations before any direct code reuse" of OpenSensorHub, and IDR-SRV-014B §17.1 records that for Connected Systems Go an "absent license text blocks confident direct source or fixture reuse." Neither is a Guide defect and neither changes this finding's disposition; both are recorded because this finding already notes that dependency and bundled-artifact notices do not select the project's own licence. Evidence: [31-pass-3c-26-batch8-peer-studies-and-source-checks.md](evidence/31-pass-3c-26-batch8-peer-studies-and-source-checks.md), Section 4.

**Saved evidence:** [01-pass-1.txt](evidence/01-pass-1.txt), starting at line 205. Consult later qualifications below where applicable.

### F-02 - Enforced repository checks

**Status:** Reported risk; baseline-specific. **Reported severity:** High.
**Affected work:** #6 / before subsequent code merges such as #7.

The review reported no enforced required-check gate at its inspected server baseline. Written assistant review/merge rules do not themselves configure GitHub enforcement. This checkpoint does not assert that live settings are unchanged.

**Next consideration:** Confirm actual controls when action is authorized; decide required CI enforcement. Do not add a mandatory human-review gate or change settings automatically.

**Saved evidence:** [01-pass-1.txt](evidence/01-pass-1.txt), starting at line 214. Consult later qualifications below where applicable.

### F-03 - Minimal authorization semantics

**Status:** Narrowed clarification candidate. **Reported severity:** Low-Medium.
**Affected work:** #22.

Issue #22 already requires an independent action/source/resource matrix, allowed/denied/unavailable cases and fault checks. The residual question is selected action/scope/inheritance/source-binding/concealment semantics, not absence of an authorization model or tests.

**Next consideration:** Make the selected semantics explicit enough for independent expectations. A universal policy engine and research-specific approval/lease machinery are not automatically needed.

**Related instance (iteration 16, IDR-038 command authorization/safety/audit matrix):** IDR-SRV-038 Appendix A describes a fuller decision architecture than the Guide adopts: a separation-of-duties "operator approval" gate, an independent "override" authority bound to non-overrideable rules with compensating controls, and an exclusive/coordinated "target/control lease." Guide v1.3 and the Goal are both silent on `lease`, `operator approval`/`separation of duties` and command-safety `override` (confirmed by targeted search of both documents). This is the same category of research-recommended machinery this finding already treats as optional rather than a required-and-missing control; the Guide's continued silence is a project choice already accounted for here, not a new departure. No Guide correction; no new finding. Evidence: [21-pass-3c-16-research-deep-read-slice1.md](evidence/21-pass-3c-16-research-deep-read-slice1.md), Section 5.

**Related instance (iteration 17, response cache validators and variants) — an evidenced gap, not an unadopted option.** Unlike the instance above, this one concerns the Guide's own concealment commitment and needs no policy model to fix. IDR-SRV-040 §10.4 requires every representation cache key and validator to include the authorized-view identity and forbids a shared cache serving one requester's representation to another context; its §16.3 invariant 6 lists "a cache, cursor, subscription or queued decision cannot outlive its policy/trust bounds." The Guide adopts the cursor, subscription and queued-decision members (lines 471, 547, 647) and omits the cache member. It emits strong representation-specific ETags for concurrency control (line 503) and `Vary` plus representation-specific ETags for content negotiation (line 739), and names RFC 9110/9111 as an inherited dependency (line 258), but nowhere binds a validator or cache key to the authorized view, specifies any `Cache-Control` directive, or lists a cache channel among its disclosure-leak tests (lines 1019, 983). The inherited standard does not close this: RFC 9111 §3.5 restricts shared-cache reuse only for requests carrying an `Authorization` header, §5.2.2.7's `private` directive is never invoked by the Guide, and §4.1 keys `Vary` selection only on nominated request header fields. Two concrete consequences follow. First, if the ETag is computed over stored resource state rather than the emitted authorized representation, a caller whose view conceals a field can detect concealed changes by polling, because the visible body is unchanged while the validator changes — the same leak class the Guide already commits to closing at lines 469, 803 and 975. Second, in the trusted-reverse-proxy deployments the Guide supports (lines 364, 596), where a caller's authorization derives from a client certificate or a proxy-injected identity header rather than an `Authorization` header, an intermediary may serve one caller's authorized representation to another context. Evidence: [22-pass-3c-17-research-deep-read-slice2.md](evidence/22-pass-3c-17-research-deep-read-slice2.md), Section 5.

**Corroboration and a second consequence (iteration 18, IDR-042).** The batch that read IDR-SRV-042's remainder was directed to re-test this gap against it. The gap is reinforced, not qualified. IDR-042 §7 closes its resource-family table with the rule the Guide does not state: "'Cache-safe' means policy-partitioned, integrity/version checked and accompanied by correct validators; it does not mean publicly cacheable. Protected or principal-specific responses use appropriate private/no-store controls" (line 341, tagged **[N/A/P]**), and its §3.1 lists RFC 9111 §§4.2-4.2.4 and 5.1-5.2 as a normative source (line 136). Two accepted reports therefore state the requirement, partly on a normative-derived rather than purely project-chosen basis. IDR-042 also adds a second consequence the earlier statement did not cover: HTTP cache freshness must not be read as domain freshness (§4.2 line 197; §6.4 line 313, "`ageSeconds` is domain-evidence age under the named rule, not HTTP `Age`"), with two ready-made API fixtures at §15.1 lines 577-578 ("HTTP cache fresh/domain stale → `Age` and domain assessment remain independent"; "HTTP cache stale/domain fresh → cache revalidation behavior does not rewrite domain time"). The Guide establishes the underlying domain distinction thoroughly (lines 147, 481, 970) but states no HTTP-layer caching rules at all, so this is the same single omission showing a second consequence rather than a separate defect. Evidence: [23-pass-3c-18-research-deep-read-slice3.md](evidence/23-pass-3c-18-research-deep-read-slice3.md), Section 2.

**Third corroborating source (iteration 21, IDR-034).** IDR-SRV-034 Section 13.4 closes with "Principal-specific results use partitioned/private caches." Evidence: [26-pass-3c-21-idr030-completion-and-idr034.md](evidence/26-pass-3c-21-idr030-completion-and-idr034.md), Section 3.3.

**The owning layer (iteration 34).** §8.1's layer table names the layer that would own the check: "Recovery/security/performance | **Access isolation**, resource limits, interruption, replay, uncertain effects." A cache serving one authorized view to another context is an access-isolation failure, so the layer exists and is already staffed with fault injection. No cache or validator channel is named there either, which is the fourth independent place in the Guide where the enumeration omits it. That is consistent rather than contradictory: the Guide does not carry the rule anywhere, so it appears in none of its channel lists. Disposition unchanged. Evidence: [39-pass-3c-34-batch16-verification-quality.md](evidence/39-pass-3c-34-batch16-verification-quality.md), §4.

**A remedy location (iteration 33).** The scenario pass gives this instance somewhere concrete to land. Guide §8.2 contains no occurrence of `cache`, `ETag`, `conditional`, `If-None-Match`, `Vary` or `Cache-Control`, which is a third independent confirmation after line 739 and the disclosure-test lists, and §9.1's leak row enumerates the same channels without the cache. The useful half is positive: scenarios 8 and 9 each **end** with an indirect-disclosure clause, "membership, counts and errors must not reveal those facts" and "must not alter the permitted response or reveal them indirectly." Those clauses are where the project decided to enumerate disclosure channels, so extending them is the edit, not adding a scenario. The Guide does test preconditions, at line 511 under writes, but for concurrency rather than for binding a validator to the authorized view. Disposition unchanged. Evidence: [38-pass-3c-33-batch15-scenario-pass.md](evidence/38-pass-3c-33-batch15-scenario-pass.md), §4.

**Fourteen sources, and the rule stated in this finding's own terms (iteration 29).** Batch 11 found the requirement written as a numbered step in a report about geospatial storage. IDR-SRV-026 §13.1, its Policy Decision Order, runs: authenticate and resolve the principal; select candidate versions within policy partitions; evaluate access to the exact fact and the permissible disclosure product; choose the representation; apply predicates to that contractually defined view; and then, at step 6, "Compute sort, counts, extents, pagination, links, and cache entries from the authorized view." This finding says the Guide "nowhere binds a validator or cache key to the authorized view." That is the same sentence, and an accepted report states it as a required ordering.

Three more sources arrived with it. IDR-026 recommendation 10: "Apply policy before geometry selection, predicates, counts, extents, sorting, paging, caching, and export. Test inference channels and derived-policy invalidation." IDR-SRV-027 P-027-14: "Apply policy before latest/extents/counts/aggregates/pages/streams/archives and scope derived products/caches to policy/version," which reaches the IDR-025 mechanism independently, and whose risk register supplies the invalidation half, "keyed invalidation, watermarks, rebuild/failure tests." IDR-SRV-028 §4.2: "Put policy before index, search, diagnostics, links, rendering, caching, and download," with its risk register naming the failure, "Search/cache crosses policy boundaries | existence/content disclosure | policy-built documents, scoped keys/indexes, leakage tests."

That makes fourteen accepted reports across seven unrelated subject areas: security, policy, testing, content negotiation, data contracts, storage and geospatial query. Substance, severity and remedy are unchanged and no escalation is proposed. The accumulation is recorded because it bears on how a consolidation owner weighs a Low-Medium item. This is not one report's preference that the Guide declined. It is a rule that every research area touching derived data states on its own initiative, and that Guide line 739 does not carry. Evidence: [34-pass-3c-29-batch11-storage-query-and-write-boundaries.md](evidence/34-pass-3c-29-batch11-storage-query-and-write-boundaries.md), §5.

**Ten sources, a stated mechanism and the first named owners (iteration 28).** Batches 9 and 10 changed what is known about this instance without changing what it says. Until now its sources were reports about security, policy, testing and content negotiation, which left open whether the requirement was an artifact of those subject areas. Four reports whose subject is data contracts and storage state it independently. IDR-SRV-021 R-021-15 (Critical): "Apply policy before projection and secure raw artifacts, nested metadata, indexes, links, errors, resolvers, and caches independently." IDR-SRV-022 R-022-14 (Critical): "Apply resource budgets and policy to schemas, values, references, codecs, indexes, errors, logs, caches, generated views, and examples." IDR-SRV-025 recommendation 15 (Critical): "protect backups, replication, caches, indexes, broker state, diagnostics, and deduplication side channels."

IDR-025 recommendation 8 (High) states the mechanism the earlier sources only implied: "Make caches and materialized views disposable and evidence-bearing. Record source watermark, builder/profile/policy version, staleness, and deterministic rebuild." Recording the policy version with the cached representation is what binds a cache to the authorized view, which is the thing this instance says the Guide never states.

And the question now has owners. IDR-SRV-018 §16.2 routes downstream: "Which HTTP cache directives apply per mutable, immutable, authorized, and disconnected response class? IDR-SRV-045/046." IDR-SRV-020 §17.1 adds "policy-aware cache keys" as the control against a visible aggregate being affected by hidden evidence. So the instance rests on ten accepted reports across five unrelated subject areas, it has a stated mechanism, and two topics were expected to settle its HTTP-layer part. Substance, severity and remedy are unchanged and no escalation is proposed; this is recorded so the consolidation owner can see the convergence rather than rediscover it. Evidence: [33-pass-3c-28-batch9-completion-and-batch10.md](evidence/33-pass-3c-28-batch9-completion-and-batch10.md), §§1.3 and 2.4.

**Sixth source, and a change of character (iteration 25, IDR-012).** This instance previously rested on general architectural principles drawn from security and policy reports, and the Guide's silence could fairly be called unspecified. IDR-SRV-012, the accepted content-negotiation report, owns the subject matter of Guide line 739 and changes that. Its §7.6 states: "`Vary` is not an authorization control. Until IDR-SRV-039/040 proves that a response is identical public content for all callers, authenticated or policy-dependent responses should default to `Cache-Control: private, no-store`." Its numbered recommendation 9 issues that as a single instruction: "use correct `Vary` and representation/coding-specific validators; and default protected variants to `private, no-store` until security/policy research permits otherwise." Its required test list includes "representation-specific ETags, conditional requests, private/no-store defaults" as one row. **Guide line 739 adopts the first half of that one recommendation and omits the second:** "Use appropriate `Vary` headers and representation-specific ETags." The instance is therefore a partial adoption of a single accepted recommendation rather than an unspecified area, and IDR-012 also states directly the point iteration 17 had to derive from RFC 9111 §4.1. It now rests on six accepted reports. Substance and remedy unchanged; the evidentiary change is recorded so the owner can weigh it at consolidation. Evidence: [30-pass-3c-25-batch7-key-section-screen-b.md](evidence/30-pass-3c-25-batch7-key-section-screen-b.md), Section 2.

**Fifth source, from the test side (iteration 23, IDR-055).** IDR-SRV-055 is the first source to express the rule as a *testable leakage surface* rather than an architectural obligation. Its Section 9 leakage inventory names "status, headers, response length, cache validators and timing"; its Section 8 requires that "Cursors and validators bind the authorized view, policy version, principal/client and query/filter"; its required-matrix row `SEC-POLICY-VIEW` gives the negative case as "hidden row changes count/page/latest/ETag"; and its implementation proof 4 requires twin-world tests to prove the public projection "including count, page, extent, latest, cursor and ETag" does not change with hidden facts. That is precisely the test formulation this instance's remedy already asks to add to the Guide's disclosure-channel list at line 1019. Evidence: [28-pass-3c-23-idr055.md](evidence/28-pass-3c-23-idr055.md), Section 4.2. **Corrected in iteration 24:** iteration 23 qualified this as corroboration only because it believed IDR-055 was unaccepted. IDR-055 was accepted on September 16, 2026, so this is a fifth **accepted** source and the instance now rests on five. Substance and remedy unchanged. Evidence: [29-pass-3c-24-idr055-acceptance-correction-and-batch6.md](evidence/29-pass-3c-24-idr055-acceptance-correction-and-batch6.md), Part A3.3.

**Fourth source, and the first naming ETags (iteration 22, IDR-039A).** IDR-SRV-039A states the rule twice and is the first source to name the validator explicitly. Section 11.3: "Counts, extents, ordering, pagination, cursor validity, ETags, `Location`, alternate representations, response timing classes and error existence behavior describe only the authorized view. Cache keys include that view and relevant policy/evidence versions." Its Section 9.5 enforcement-point registry lists a "Cache/cursor/ETag PEP" whose required behaviour is to "bind to authorized-view and policy/data/security versions." Iteration 17 had derived the validator half of this instance from RFC 9111 mechanics rather than from any research statement, and noted that the Guide's "representation-specific" wording at lines 503 and 739 left it ambiguous whether an authorized view counts as a distinct representation. That ambiguity is now removed on the research side. The instance rests on four accepted reports: IDR-040 Section 10.4, IDR-042 Section 7 line 341, IDR-034 Section 13.4 and IDR-039A Sections 9.5 and 11.3. Its substance and recommended fix are unchanged. Evidence: [27-pass-3c-22-idr039a.md](evidence/27-pass-3c-22-idr039a.md), Section 3.3.

**Next consideration for that instance:** State in Section 4.10 or Section 6 that validators and any cache key are bound to the authorized view, that authorization-dependent representations carry `Cache-Control: private` (or `no-store`), and that HTTP cache freshness is not domain freshness. Add the cache/validator channel to the line 1019 disclosure-channel test list, and the two IDR-042 §15.1 cache-versus-domain-freshness fixtures to the Section 8 checks. All are documentation and test changes within existing owners; no new capability, policy engine or caching layer is implied, and the choice between `private` and `no-store` remains the owner's.

**Saved evidence:** [03-pass-3a.txt](evidence/03-pass-3a.txt), starting at line 101. Consult later qualifications below where applicable.

### F-04 - Issue size

**Status:** Acknowledged planning risk; nonblocking. **Reported severity:** Low.
**Affected work:** Relevant implementation leaves / phase recalibration.

Some leaves may exceed one iteration. The Roadmap already provides splitting and recalibration rules. No demonstrated need to rewrite the entire backlog follows.

**Next consideration:** Recalibrate against implementation evidence; no automatic splitting in this review.

**Saved evidence:** [01-pass-1.txt](evidence/01-pass-1.txt), starting at line 232. Consult later qualifications below where applicable.

### F-05 - Tasking sequence

**Status:** Optional scheduling improvement. **Reported severity:** Low.
**Affected work:** #156 / group 5.1.

Current dependencies delay tasking until the observation encodings are integrated. Earlier JSON-only tasking was proposed as a scheduling alternative, not a missing capability or standards defect.

**Next consideration:** Leave as optional unless the owner changes sequencing; do not rewrite dependencies automatically.

**Saved evidence:** [01-pass-1.txt](evidence/01-pass-1.txt), starting at line 238. Consult later qualifications below where applicable.

### F-06 - Exchange subsystem lacks a consumer

**Status:** Withdrawn. **Reported severity:** Withdrawn.
**Affected work:** None.

The reviewer withdrew the premise: configured peers/files, SSE bootstrap and MQTT recovery are consumers, and Phase 8 also contains ordinary recovery work.

**Next consideration:** Preserve withdrawal. Do not resurrect the original finding after compaction.

**Saved evidence:** [03-pass-3a.txt](evidence/03-pass-3a.txt), starting at line 104. Consult later qualifications below where applicable.

### F-07 - resultTime=latest interpretation

**Status:** Interpretation/documentation concern. **Reported severity:** Medium.
**Affected work:** #112 / observation selection.

The Guide selects the maximum resultTime within the authorized, filtered endpoint scope, retaining ties. Research already chose that interpretation. Peer per-series behavior is informative, not normative; nested datastream endpoints can still contain multiple sampling-feature series.

**Next consideration:** Record the interpretation and relevant interoperability limits clearly; use discriminating fixtures. A semantic change or upstream filing is not authorized.

**Saved evidence:** [03-pass-3a.txt](evidence/03-pass-3a.txt), starting at line 107. Consult later qualifications below where applicable.

### F-08 - Abstract-test prerequisite conflicts

**Status:** Narrowed interpretation/documentation concern. **Reported severity:** Low-Medium.
**Affected work:** #80 and Part 2 inheritance follow-up.

The prerequisite conflict was already recorded in research; the original claim that it was unrecorded everywhere was withdrawn. Annex A is normative, so an adapted procedure needs an explicit qualification.

**Next consideration:** Carry the selected treatment into test evidence; distinguish original from adapted abstract-test outcomes. The Part 2 inheritance check was closed as supported in iteration 14 (related instance below); the requested Section 13 row should be worded for both Parts.

**Related instance (iteration 12, SWE Common 3.0):** Abstract Tests A.85 `/conf/json-encoding-rules/record-object-valid` and A.86 `/conf/json-encoding-rules/vector-object-valid` in OGC 24-014 prescribe test methods that check only the JSON-object form of DataRecord/Vector values. Array-form values (`recordsAsArrays`/`vectorsAsArrays` true) therefore cannot pass the literal test method even though Requirement 85C/86C defines their rules; any Glaux evidence for array-form values is an adapted check and must be labelled as such. Owning work: the conformance-claim leaves assembling `swecommon-json` evidence (Guide Section 7 row for `swecommon-json`; Phase 9 reverification), not the codec leaf 4.1.3 (#130), whose tests are adequate. Evidence: [17-pass-3c-12-standards-swe.md](evidence/17-pass-3c-12-standards-swe.md), Section 3.3.

**Related instance (iteration 13, CQL2 and Features Part 3):** In OGC 21-065r2 Annex A the tests "against the test dataset" (A.3.5, A.3.6, A.7.2, A.8.2) are conditional on hosting the Natural Earth feature collections, which an observation-only server cannot host as Features collections; they must be recorded as not applicable (conditional), never as passed. No abstract test exercises a singleton GeometryCollection, so the Guide Section 13 row 1162 acceptance is a Glaux adaptation that only Glaux fixtures (leaf 7.3.3, #231) can evidence. Conversely, A.3.2 `/conf/basic-cql2/comparison` evaluates all six comparison operators against every String and Boolean queryable and expects success, and A.7.1/A.8.1 expect `POINT(90 180)` to fail: these are official expectations the runner must implement literally, not adaptations. Owning work: 7.3.1-7.3.4 (#229-#232), 7.2.6 (#228) and the Phase 9 claim leaves. Evidence: [18-pass-3c-13-standards-cql2-sensorml.md](evidence/18-pass-3c-13-standards-cql2-sensorml.md), Sections 2.5-2.6.

**Related instance (iteration 14, Part 2 inheritance and the IDR-011 abstract-test overlay):** OGC 23-002 Clause 8 `/req/api-common` and Annex A.1 `/conf/api-common` both name the Part 1 `api-common` class as prerequisite, and every Part 2 resource conformance class (A.2-A.6) lists Part 2 `/conf/api-common`; so every Part 2 declaration inherits the Part 1 Annex A prerequisite conflict already covered by this finding, and Part 2 adds no further inherited class beyond the Guide Section 1.2 table. Part 2 A.1 has no test of its own; A.2 runs eighteen Features Part 1 core tests only for exposed collections whose `itemType` is not `feature`, and Part 2 collections are optional, so Part 2 `api-common` evidence must be labelled as the inherited Part 1 result plus conditional A.2 tests, never as a vacuous pass. Separately, all nine remaining IDR-011 Section 14.3 rows were confirmed verbatim in the published Annex A text (23-001 A.65; 23-002 A.31/A.33, A.35, A.36, A.40/A.42, A.43, A.50, A.61, A.62): the corrected procedures Glaux runs for those tests are adapted checks against the normative requirements and must be labelled as such, and where the official test is vacuous (Part 2 canonical-URL tests without exposed collections) or silent (`latest`, `limit`/`datetime` on status and result endpoints) the supplement is a Glaux check, not an official pass. Owning work: 2.6.1-2.6.3 (#80-#82), 3.5.1-3.5.2 (#121-#122), 5.5.16 and the Phase 9 claim leaves. Evidence: [19-pass-3c-14-standards-ats-rows-part2-inheritance.md](evidence/19-pass-3c-14-standards-ats-rows-part2-inheritance.md), Sections 2-3.

**Saved evidence:** [03-pass-3a.txt](evidence/03-pass-3a.txt), starting at line 113. Consult later qualifications below where applicable.

### F-09 - deployedSystems abstract-test assumptions

**Status:** Source-conflict / test-adaptation concern. **Reported severity:** Low.
**Affected work:** #81.

Some abstract-test steps presume collection dereferencing, while the selected mappings use inline associations. The claimed normative /deployments/{id}/systems route was withdrawn.

**Next consideration:** Document the specific adaptation and independent association expectation. Do not add an invented route or report an adapted test as an unmodified official pass.

**The owning task carries the guard (iteration 39).** This finding's recorded affected work is issue #81, and its task content was assessed in batch 21. It forbids inferring “routes by appending item IDs to every nested list” and forbids importing “server traversal logic into the conformance runner”, and it requires that “deliberately wrong parents or missing descendants are detected rather than copied from server output”. Combined with Guide line 909, which iteration 34 found already carries the general rule, the guard against the wrong inference is in place. What neither supplies is the documented adaptation itself: no task names the `deployedSystems` step as a correction recorded under line 909. The residual is therefore narrower than before and not closed, and it remains fixture content owned by #81 rather than a Guide gap. Disposition unchanged. Evidence: [45-pass-3c-39-batch21-task-assessment.md](evidence/45-pass-3c-39-batch21-task-assessment.md), §4.

**The general rule is already adopted (iteration 34).** Guide line 909 carries the second half of that consideration in the Guide's own words: "Implement the published abstract tests as independent HTTP checks where possible. Extend them with negative and semantic tests when an abstract test is weak, ambiguous, or copied incorrectly. **Preserve both the source procedure and the documented correction; never silently turn a server defect into a passed standard test.** Distinguish unimplemented, not run, failed, harness error, and genuinely inapplicable cases." Line 930 applies the same pattern concretely to Part 4, and §7.3 adds that passing extension tests adds no approved conformance URI.

So what remains under this finding is the **specific** documented adaptation for the `deployedSystems` abstract-test steps, which is fixture and test content owned by issue #81 rather than a Guide gap. Disposition unchanged. Evidence: [39-pass-3c-34-batch16-verification-quality.md](evidence/39-pass-3c-34-batch16-verification-quality.md), §3.2.

**Saved evidence:** [03-pass-3a.txt](evidence/03-pass-3a.txt), starting at line 116. Consult later qualifications below where applicable.

### F-10 - Malformed research source links

**Status:** Reported reproducibility defect. **Reported severity:** Low.
**Affected work:** IDR-011 source links / affected conformance tasks.

Nine IDR-011 links reportedly use a malformed shortened commit hash. The checked Guide/Roadmap pins were not the affected links.

**Next consideration:** Correct the specific links when authorized; retain the pinned source rather than substituting a moving branch.

**First verified instance (iteration 26, IDR-040).** This finding previously rested on a reported defect in nine IDR-011 links. A second, independently verified instance now exists in a different report. IDR-SRV-040 §21.3 cites Connected Systems Go at `opensensorhub/connected-systems-go`, which returns **Not Found** from the public API. `SomethingCreativeStudios/connected-systems-go` exists, is not a fork and was last pushed on 2026-09-02, and is the URL used by IDR-034 §19.3, IDR-037 Appendix B, IDR-038 Appendix C, IDR-039 §22.2, IDR-055 §19.3 and Guide line 1129. The single IDR-040 citation is therefore the incorrect one. Iteration 17 recorded this as an unverified follow-up question and deliberately declined to attach it here; that reservation is now discharged. This is a citation defect in one accepted research report, not a Guide defect, and the Guide's own citation is correct. Evidence: [31-pass-3c-26-batch8-peer-studies-and-source-checks.md](evidence/31-pass-3c-26-batch8-peer-studies-and-source-checks.md), Section 2.1.

**Saved evidence:** [02-pass-2.txt](evidence/02-pass-2.txt), starting at line 143. Consult later qualifications below where applicable.

### F-11 - Relation wire spelling

**Status:** Interpretation/documentation concern. **Reported severity:** Medium.
**Affected work:** #56 / relationship and conformance fixtures.

The standard's tables, mappings and examples have conflicting relation spellings. Issue #56 already checks independent expected edges, relation meaning and canonical URLs: missing navigation tests is not the finding.

**Next consideration:** Document the selected spelling/compatibility rule in existing fixtures. Do not prefix unrelated IANA relations or JSON properties or case-fold target URLs.

**Confirmed at Guide v1.3 (iteration 25).** Both sides are now evidenced. The accepted research sets a controlling interpretation: IDR-SRV-010 Appendix C row C-12 records "Encoding sentence/examples/ATS disagree with `ogc-rel:` mapping intent | Prefixed table-first output | Bare-input compatibility; named ATS adapter," and row C-13 adds that lexical case differences must be treated as one equivalent relation. The Guide states no such rule: it contains no occurrence of `ogc-rel`, and §13 carries no row for relation spelling. It nevertheless emits relations in three different forms. The System Event parent mapping is "an authorized `links` entry with `rel: "system"`" (line 485), a bare name. The AsyncAPI description uses "extension relation `urn:glaux:rel:experimental-asyncapi`, never a fabricated OGC relation" (line 560), a Glaux URN. Queryables use "the `http://www.opengis.net/def/rel/ogc/1.0/queryables` link" (line 774), a full URI. The bare form at line 485 is the one that diverges from C-12, under which a bare name is an accepted input form rather than the emitted output. Whether that divergence is a defect turns on which published reading controls, which is the question this finding records as unresolved and which iteration 25 did not resolve. What is established is that the rule this finding asks for is still absent at v1.3, that the Guide is internally inconsistent in the forms it emits, and that one form contradicts the accepted interpretation. Evidence: [30-pass-3c-25-batch7-key-section-screen-b.md](evidence/30-pass-3c-25-batch7-key-section-screen-b.md), Section 3.

**A remedy location (iteration 33).** This finding's recorded next consideration is to document the selected spelling rule in existing fixtures. The scenario pass names the fixture: Guide §8.2 scenario 1 requires following links from the root and recovering "the same identities and associations in each supported representation," which exercises navigation but not the spelling of the relations, and §8.2 contains no `ogc-rel` and no `rel:`. Disposition unchanged. Evidence: [38-pass-3c-33-batch15-scenario-pass.md](evidence/38-pass-3c-33-batch15-scenario-pass.md), §4.

**The rule stated, in an accepted report (iteration 27, IDR-017).** The relationship and linkage model states the rule this finding asks for, as a numbered recommendation, and the Guide neither carries it nor mentions it. **R-017-08** (line 764, priority High, basis N/X/IDR-010): "Emit exact Part 1 Table 3 `ogc-rel:` spellings, compare extension relation URIs case-insensitively, and confine bare values to a named compatibility adapter." Its §9.3 states the operative consequence at line 464: "Known bare forms seen in tagged examples may be accepted only by a named compatibility adapter with telemetry and fixtures; Glaux does not emit duplicate bare and prefixed links." That agrees with IDR-010 row C-12 and sharpens it from an interpretation into an instruction. The Guide contains no occurrence of `ogc-rel`, states no spelling rule, carries no §13 row for it, and emits a bare `rel: "system"` at line 485, which R-017-08 would confine to a named compatibility adapter rather than to ordinary output.

This is the same shape as the F-03 result from iteration 25: a specific, numbered, accepted recommendation the Guide neither carries nor mentions, with an observable divergence at a named Guide line. It does not resolve this finding's open question, which is which published reading controls. It establishes that the accepted research answers that question one way and that Guide line 485 goes the other way, which is materially stronger than the position recorded in iteration 25.

A related half-adoption was recorded here in iteration 27 and is **withdrawn in iteration 28**. It read R-017-09, which asks for "a versioned HTTPS Glaux relation namespace for Part 2 navigation gaps; advertise and test it as a project extension, never an OGC claim," as half adopted, because Guide line 560 carries "never a fabricated OGC relation" in the same words but uses the URN `urn:glaux:rel:experimental-asyncapi` rather than an HTTPS namespace. Reading IDR-017 §17.2 shows that was too strong. It lists among questions assigned downstream and not blocking acceptance: "Final Glaux relation namespace URI and published vocabulary lifecycle | IDR-SRV-014/046/047 before implementation." The report does not fix the namespace form; it defers it to named owners with a stated deadline. The Guide's URN is therefore a choice made inside an open question, not a departure from a settled rule, and the half-adoption characterization falls away. **R-017-08 is unaffected and the result above stands in full**, because it is stated as a recommendation with no corresponding open question. Two rows add support without changing anything: §17.1 pairs the risk of "Overloading `alternate` or bare relation words" with the control "exact registry and project namespace," and §17.2 routes "Whether later standards maintenance resolves Table 3 and Part 2 relation gaps" to the upstream register and IDR-SRV-057, which is the clearest statement yet that this finding's unresolved question has an upstream component no Glaux decision can close. Evidence: [33-pass-3c-28-batch9-completion-and-batch10.md](evidence/33-pass-3c-28-batch9-completion-and-batch10.md), §1.3. Substance and remedy unchanged. Evidence: [32-pass-3c-27-batch8-corrections-and-batch9.md](evidence/32-pass-3c-27-batch8-corrections-and-batch9.md), Section 3.4.

**Support from the peer-study side (iteration 27).** IDR-SRV-014G §14.3, line 474, asks: "Will Glaux provide any explicitly noncanonical compatibility adapter, and under what namespace/profile?" That is this finding's question asked from outside, naming the same two unknowns, the adapter and the namespace. Evidence: [32-pass-3c-27-batch8-corrections-and-batch9.md](evidence/32-pass-3c-27-batch8-corrections-and-batch9.md), Section 2.3.

**Further support (iteration 26).** Two of the peer studies independently call for the same canonical treatment. IDR-SRV-014F recommendation 3: "Use canonical typed link relations even where generic `data` is technically permitted; test link-order independence in external clients." IDR-SRV-014D R-014D-004: "Preserve canonical relationship semantics. Use typed relation identifiers and verify root/nested paths resolve to the same resource identity." The Guide still states no relation-spelling rule. Substance and remedy unchanged. Evidence: [31-pass-3c-26-batch8-peer-studies-and-source-checks.md](evidence/31-pass-3c-26-batch8-peer-studies-and-source-checks.md), Section 4.

**Saved evidence:** [03-pass-3a.txt](evidence/03-pass-3a.txt), starting at line 137. Consult later qualifications below where applicable.

### F-12 - Independent response interpretation

**Status:** Narrowed optional hardening. **Reported severity:** Low.
**Affected work:** #19 and runner extensions such as #80.

The Guide already specifies independent expectations and prohibits implementation logic as the sole oracle. The residual concern is using production wire types to interpret supposedly independent responses.

**Next consideration:** If useful, clarify the response-interpretation boundary and a discriminating negative fixture. A new crate, particular runner tool or dependency graph alone is neither required nor sufficient.

**A per-issue counterpart (iteration 35).** Every one of the 286 implementation issues carries a field headed "Verification approach and independently expected results," present without exception. That is where this finding's requirement is satisfied or violated for each task, and it is the per-issue counterpart to Guide line 901. The shared completion checklist reinforces it in every issue: "Applicable tests use independent expected results and demonstrate detection of relevant wrong behavior; behavioral red-green evidence or an appropriate alternative is recorded." So the independence rule is built into the unit of work as well as stated in the Guide, which bears on how a consolidation owner weighs the residual. Disposition unchanged. Evidence: [41-pass-3c-35-batch17-leaf-to-issue-comparison.md](evidence/41-pass-3c-35-batch17-leaf-to-issue-comparison.md), §§3 and 5.

**A conflation corrected, and the counts withdrawn (iteration 39).** Iterations 37 and 38 presented tallies of tasks said to state “the actual-side independence boundary”, 12 of 55 assessed against 28 of 286 by pattern, and concluded the pattern under-detects by roughly half. **Those counts and that conclusion are withdrawn.** They ran two different rules together.

The first is expected-answer independence: do not derive the expected answer from the server’s own output. That is what nearly all the cited wording says, including “not by converting the serializer’s output into an oracle” and “independently of server decoding”. The second is this finding’s residual: whether a runner may interpret the *actual response* by deserializing it into the production crate’s wire types while holding an independently authored expectation. Almost none of the cited wording addresses that.

Since neither tally counted the boundary in question, no ratio between them supports a claim about detection, and no replacement census is needed; a corpus count was never going to answer it. The quoted examples remain accurate and are retained as observations about oracle discipline in the work instructions, not as a measurement of this finding.

**This finding is unchanged and is not weakened.** It rests on the Guide-text analysis recorded in iteration 34: Guide line 901 forbids the server’s serializers *as the sole oracle* and permits *ordinary* format libraries, and a production DTO is neither, so the sentence leaves the question open. That basis never depended on the withdrawn counts. Evidence: [45-pass-3c-39-batch21-task-assessment.md](evidence/45-pass-3c-39-batch21-task-assessment.md), §1.

**Quantified, and pointed at the remedy (iteration 37).** The Phase 1 task assessment measured how often the work instructions state the boundary this finding records. Six of 24 tasks state it explicitly in their own words, most precisely issue #14: “The expected bytes must not come from the server’s reserialization,” with “a successful round-trip through the same faulty conversion is insufficient.” Issue #20 requires constructing tokens “independently of server decoding,” and #16 authoring the expected-outcome table “independently of the locking code.”

Corpus-wide the boundary appears in **at least 24 of the 286 issues**, and that is a lower bound demonstrated twice inside one batch: a first pattern found four, a broadened pattern found 24, and three further instances in Phase 1 fell outside even that. A separate 66 issues use expected-value independence wording, which constrains where the expectation comes from but not how the actual response is decoded, and is a different rule.

**This is the strongest support the finding has had, and it argues for the recorded remedy rather than against it.** The rule is real and the project clearly believes in it, but it is applied where an issue author happened to think of it, concentrated in foundational tasks. One clause on Guide line 901 would apply to all 286 uniformly. A correction follows for iteration 35, which recorded the shared boilerplate as carrying the Guide’s verification practices into every issue: that holds for the false-green rule and for independent expected results, and not for this boundary, since the checklist’s phrase is “use independent expected results”, which is the expectation side. Disposition unchanged. Evidence: [43-pass-3c-37-batch19-phase1-task-assessment.md](evidence/43-pass-3c-37-batch19-phase1-task-assessment.md), §4.

**Relocated, and sharpened (iteration 34).** The verification pass found this finding's real home, and it is not in the testing strategy. **Guide line 901** is the controlling rule: "The conformance runner must not import server selection logic, serializers, or fixture-derived expected responses as its sole oracle. It may use ordinary HTTP/format libraries and pinned schemas, but expected resource relationships, query answers, and value meaning must be independently authored or calculated."

Reading it exactly narrows the residual to a question the sentence does not settle. It forbids resting on the server's serializers **as the sole oracle**, and it permits **ordinary** format libraries. A production crate's typed structures are neither: not the sole oracle, since the expected values are still independently authored, and not an ordinary format library either. So whether the runner may deserialize a response into the production types is left open by both halves of the sentence.

The consequence is concrete and small. If the runner decodes with the production types, a field those types silently drop or coerce becomes invisible to the check, and the independently authored expectation is compared against an already-normalized value. §8.1.1 states exactly this logic one layer down, "Encoder/decoder agreement alone cannot exclude a shared bug"; what is missing is its application to the HTTP runner's response interpretation.

Disposition unchanged: narrowed optional hardening, Low severity. The finding is better supported and more precisely located than this register had recorded, and the recorded next consideration is one clause added to a sentence that already draws the line in the right place. Evidence: [39-pass-3c-34-batch16-verification-quality.md](evidence/39-pass-3c-34-batch16-verification-quality.md), §3.1.

**Two more accepted sources (iteration 31), now four.** Two reports state the boundary from their own side. IDR-SRV-053's risk register: "fixture output copied from server | circular tests bless defects | independent expected facts and four-role separation," its four roles being source, scenario, oracle and generated evidence. IDR-SRV-056 recommendation 6: "Treat curl/Rust transcripts as attribution oracles, not extra semantic-client votes." The Guide already carries the rule at line 955: "Independent reasoning and source data matter, not merely a separate file or assistant." Disposition unchanged; this stays a narrowed optional hardening at Low severity, now with four accepted reports stating the boundary in operational terms. Evidence: [36-pass-3c-31-batch13-operations-fixtures-performance-interop.md](evidence/36-pass-3c-31-batch13-operations-fixtures-performance-interop.md), §4.

**First peer-study support (iteration 27).** Two independent client studies state this boundary and name the artifacts that keep it. IDR-SRV-014E §15.2, line 514, pairs the risk "Parsed tools create false findings" with the control "Raw transport capture is primary oracle." IDR-SRV-014F §15.2, line 502, pairs "Server blamed for client/harness error" with "Preserve raw wire, parser output, code expectation, and standard anchor." Between them they separate the four things this finding asks the project to keep apart: the bytes on the wire, what a parser made of them, what the test expected, and what the standard requires. Disposition unchanged; this stays a narrowed optional hardening at Low severity. The value is that whoever writes the clarification now has two accepted sources stating the boundary in operational terms. Evidence: [32-pass-3c-27-batch8-corrections-and-batch9.md](evidence/32-pass-3c-27-batch8-corrections-and-batch9.md), Section 2.3.

**Saved evidence:** [05-pass-3c-01.txt](evidence/05-pass-3c-01.txt), starting at line 117. Consult later qualifications below where applicable.

### F-13 - foi@id example versus samplingFeature@id

**Status:** Source-example hazard confirmed against the published Standard; inbound unknown-member policy is a project choice to record (documentation gap), not a standards obligation. **Reported severity:** Low.
**Affected work:** 3.2.1 (#98), 3.2.2 (#99), 3.2.4-3.2.5 (#101-#102); Guide Section 4.6 (one sentence) and Section 13 (one row).

An extra foi@id member can be schema-permitted without establishing the standard samplingFeature@id association. The example's data URL is a separate issue. Rejecting every unfamiliar @id/@link member was not approved.

**Resolution of the controlling extension-rule check (iteration 15):** No published text imposes an extension-member rule for Observation JSON. OGC 23-002 Requirement 97 requires validity against `observation.json`, and both the tagged and published copies of that schema carry no `additionalProperties` or `unevaluatedProperties` keyword, so extra members are permitted by schema silence; Requirement 98 and tests A.66/A.67/A.81/A.82/A.97/A.98 never examine them, and no processing model (preserve, ignore, reject) is defined. The `@id`/`@link` association names are a convention of the schema member names and the Part 1 mapping tables, reserved by no requirement or schema rule. RFC 7946 Section 6.1 (foreign members MAY be used; "no normative processing model for foreign members is defined") governs GeoJSON documents only, and 23-002 Clause 7.2 places observations outside GeoJSON; 23-000 Requirements 9/10 govern the SensorML `extension` slot only. The published Clause 16.1.5 example's `foi@id` is confirmed and sharpened: Table 7 gives the Observation only `datastream`, `samplingFeature` and `procedure` associations, so the member maps to no association at all and a client copying the example loses the sampling-feature link and every `foi` filter match. Because no published rule requires rejection, none is prescribed; the earlier suggestion to reject or warn on unmapped `@id`/`@link` members is an owner option that would be a labelled Glaux restriction. The precise gap: Guide lines 406/432 (preserve permitted extensions; validated extensible payload), 497 (PATCH rejects changes outside the writable projection) and 713 (only advertised extension fields appear publicly) leave the outcome for a member that is neither mapped nor an advertised extension unstated for POST, PUT and PATCH. Evidence: [20-pass-3c-15-observation-extension-policy.md](evidence/20-pass-3c-15-observation-extension-policy.md), Sections 2-4.

**Next consideration:** Record in Section 4.6 the outcome for members that are neither mapped nor advertised extensions, identically for POST, PUT and PATCH (preserve-as-opaque, ignore, or reject with a problem detail naming the member; any of the three is conformant), and add a Section 13 row for the Clause 16.1.5 `foi@id` example. Keep the isolation fixtures (baseline `samplingFeature@id`; `foi@id` variant; the `data:` result URL tested separately; `foi@id` never emitted) with #99's supplied sampling-feature context checks. Reporting the example upstream remains the owner's decision.

**Saved evidence:** [06-pass-3c-02.txt](evidence/06-pass-3c-02.txt), starting at line 47. Consult later qualifications below where applicable.

### F-14 - Three interpretation details

**Status:** Qualified documentation/mapping candidates. **Reported severity:** Low.
**Affected work:** #71/#72; Deployment writes; observation listing.

(a) Root-parent semantics and known-hit tests already exist in #71/#72. (b) The reported local Deployment write mapping requires a System UID URI; canonical-URL compatibility and an additional returned uid are separate choices. (c) Research selected ascending resultTime/ID order; Guide wording should not obscure direction.

**Next consideration:** Do not rewrite existing parent tests as if absent. Preserve required UID handling separately from optional conveniences; state ordering and its client consequences.

**Confirmed instance for part (c) (iteration 21, IDR-034).** The ordering concern is now evidenced on both sides. IDR-SRV-034 states the direction twice and unambiguously: "Accepted default Observation order is `(normalized resultTime, ResourceId)` ascending" (Section 10.3) and "Default order is `(resultTime, ResourceId)` ascending" (Section 12.2). Guide line 444 names the same two fields and omits the direction: "Use deterministic ordering with a unique ID tie-breaker: result time then ID for observations; stable ID order for ordinary resource lists unless a family requires a different rule." A search of the whole Guide finds no occurrence of `ascending` or `descending`. The Guide therefore fixes the sort key and the tie-breaker while leaving the direction unstated, so two clients implementing it could page the same stream in opposite orders and both claim conformance. The fix is one word at line 444. Evidence: [26-pass-3c-21-idr030-completion-and-idr034.md](evidence/26-pass-3c-21-idr030-completion-and-idr034.md), Section 3.2.

**Saved evidence:** [06-pass-3c-02.txt](evidence/06-pass-3c-02.txt), starting at line 35. Consult later qualifications below where applicable.

### F-15 - Unkeyed command submissions

**Status:** Explicit tradeoff / documentation comparison. **Reported severity:** Low-Medium.
**Affected work:** #168 / #174; Guide command semantics.

The Guide explicitly permits unkeyed POSTs, explains ambiguous recovery and forbids assuming safe automatic resubmission. Existing issues test keyed replay and potentially distinct unkeyed actions. Research had preferred mandatory keys.

**Next consideration:** Retain the stated operational limitation. A mandatory-key deployment option is a new choice, not an already approved correction.

**Saved evidence:** [08-pass-3c-04.txt](evidence/08-pass-3c-04.txt), starting at line 27. Consult later qualifications below where applicable.

### F-16 - Batch atomicity reversal

**Status:** Withdrawn. **Reported severity:** Withdrawn.
**Affected work:** None; #104 already tests the selected policy.

The research allowed multiple transaction models; the Guide explicitly chooses bounded request-level all-or-nothing behavior, and #104 specifies rollback tests. The original claim of an unrecorded inconsistent choice was withdrawn.

**Next consideration:** Preserve withdrawal. Do not prescribe per-item success or another batch protocol.

**Saved evidence:** [08-pass-3c-04.txt](evidence/08-pass-3c-04.txt), starting at line 44. Consult later qualifications below where applicable.

### F-17 - Stale acceptance wording

**Status:** Optional editorial cleanup. **Reported severity:** Low.
**Affected work:** Affected historical reports/plans.

Dated acceptance records exist for checked reports; some closing paragraphs or headers retain earlier In Review wording. The invented rule that a header automatically controls was corrected.

**Next consideration:** If cleaning up, use the actual dated acceptance record. Do not reinterpret accepted research as unaccepted or introduce new governance precedence.

**Confirmed instance (iteration 19, IDR-043).** This finding previously rested on a reported pattern; IDR-SRV-043 now supplies a verified example with exact locations. Its header records **Report Status: Final**, **Accepted By: Glaux Project Lead** and **Acceptance Date: September 15, 2026** (lines 4 and 17-18). Section 21 nevertheless closes with "The deliverable should remain **In Review** until project-lead acceptance" (line 704), and the completion checklist's final item still reads "Category H and implementation remain unauthorized pending acceptance" (line 765). The dated acceptance record is the controlling fact and the two trailing statements are pre-acceptance residue the acceptance did not sweep. This is editorial cleanup for the owning report, not a technical defect and not a reason to treat IDR-043 as unaccepted. Evidence: [24-pass-3c-19-research-slice4-and-batch-queue.md](evidence/24-pass-3c-19-research-slice4-and-batch-queue.md), Section 3.

**Three new instances, and a withdrawn bound (iteration 29).** Batch 11 found three more, and they are the clearest form yet, because the contradiction sits inside one document rather than in a missing field. IDR-SRV-028 line 526: "The report is complete as research and is **In Review**. It is not accepted for downstream use until the Glaux Project Lead records acceptance in this report, its topic plan, and the overall plan" - while its own header, lines 16 and 17, records exactly that acceptance. IDR-SRV-032 line 495 and IDR-SRV-033 line 539 both read "Plan-owner acceptance remains deliberately unchecked while this report is **In Review**" and list the Project Lead's acceptance as a future workflow action, against headers recording it as done on September 14, 2026. A reader who checks the closing section, which is where a reader looking for status goes, is told the opposite of what the same document said in its header.

**The bound published in iterations 27 and 28 is withdrawn.** Those iterations recorded "fifteen reports checked against three instances" and concluded this was "a residue in particular reports, not a systemic practice." The count came from checking acceptance **headers**. A header check finds reports that record acceptance correctly in the header; it cannot find a report that records it correctly in the header and contradicts it in the body, which is this exact shape and was also IDR-043's shape. The method could only ever surface the IDR-055 form, and a claim about the whole population was drawn from it anyway. That was a sampling error in this review's accounting, and the reassurance drawn from it was unearned.

**The corrected population.** A single search of all 71 reports for the stale wording itself, with every hit then verified as Accepted in the governance record and carrying a dated acceptance header, gives: IDR-006 (accepted July 31, 2026), IDR-010A (August 1), IDR-014E (August 31), IDR-028, IDR-030, IDR-032, IDR-033 (all September 14), IDR-035 and IDR-036 (both September 15). With IDR-043 and IDR-055, whose wording differs and which that search does not match, the confirmed population is **at least eleven accepted reports**. It is a lower bound, not a census.

**What does not change is the disposition.** This remains optional editorial cleanup at Low severity. No technical defect follows, the dated acceptance records are the controlling facts, and nothing in the Guide depends on the stale sentences. What changes is the shape of the remedy: three scattered instances invite spot fixes, whereas eleven reports sharing a closing template invite one pass over a named list, and the list now exists. Two of the newly named reports, IDR-006 and IDR-014E, sit in batches this review screened and recorded as producing no instance; those entries are corrected rather than left to imply a clean result. Evidence: [34-pass-3c-29-batch11-storage-query-and-write-boundaries.md](evidence/34-pass-3c-29-batch11-storage-query-and-write-boundaries.md), §§2 and 3.

**Three more instances, a converged count, and the remedy (iteration 30).** Iteration 29 said its eleven was a lower bound because the search matched a fixed set of phrasings. Batch 12 confirmed that at once: IDR-044 ("The deliverable remains **In Review** until project-lead acceptance," line 705, with a matching checklist item at 773), IDR-045 (line 635 and its checklist at 769) and IDR-046 (line 905) all use wordings that search did not match. Rather than keep discovering instances one batch at a time, the scan was re-run over all 71 reports with every variant now observed, including the IDR-043 and IDR-055 forms the narrow pattern had also missed.

**Sixteen of the sixty-seven accepted reports carry stale acceptance wording:** IDR-006 (line 968), IDR-010A (1100-1108), IDR-014E (681), IDR-028 (526), IDR-029 (534), IDR-030 (772), IDR-032 (495), IDR-033 (539), IDR-035 (606), IDR-036 (649), IDR-043 (704, 765), IDR-044 (705, 773), IDR-045 (635, 769), IDR-046 (905), IDR-055 (436) and IDR-056 (585). Five are new since iteration 29. The batch produced no further variant, so the pattern has converged; the count is still a lower bound in principle, but no longer one that has moved under a different wording.

**The remedy is now fully specified, and it already exists in the corpus.** Seven reports close with the checklist item "Accepted by Glaux Project Lead." IDR-026 does better, ending with a sentence the other sixteen can copy directly: "**Acceptance record:** Accepted by the Glaux Project Lead on September 14, 2026. IDR-SRV-027 was authorized as the next bounded single-topic iteration; no later topic, draft Part 3 implementation, or server implementation was authorized." That is what each stale closing was trying to say before acceptance, written for after it, carrying the date, what the acceptance authorized, and what it did not. Fixing this finding is a replacement of one closing paragraph in sixteen named reports. No new convention, no judgement call, no technical change.

**The disposition is still unchanged.** Optional editorial cleanup at Low severity. No technical defect follows and the dated acceptance headers remain controlling in all sixteen. What the growth changes is only the size of that one editorial pass.

**A second sub-form, and the complete population (iteration 31).** Batch 13 found four reports with **no `Accepted By` and no `Acceptance Date` header fields at all**, although the governance record lists every one as Accepted. No scan built in iterations 29 or 30 could have found them. Both searched for stale closing wording that contradicts a *correct* header, and a report with no header has nothing to contradict.

This is the form that misled this review. Iteration 23 concluded IDR-055 was unaccepted precisely because its header carried no acceptance fields, and the user corrected it in iteration 24. The review then built two complete scans for a different sub-form and never returned to enumerate the one that had actually produced a wrong conclusion.

Scanning all 67 accepted reports for both forms gives **twenty affected**:

| Form | Count | Reports |
|---|---|---|
| A: stale closing wording, header correct | 14 | IDR-006, 010A, 014E, 028, 029, 030, 032, 033, 035, 036, 043, 044, 045, 046 |
| B: no acceptance header fields | 4 | IDR-051, 052, 053, 054 |
| Both | 2 | IDR-055, IDR-056 |

**The severity ordering changes, because Form B is not uniform.** IDR-051 and IDR-052 carry a dated "Accepted by Glaux Project Lead on September 16, 2026" in their closing checklists, so the status is discoverable, just not where other reports put it. **IDR-053 and IDR-054 are the worst case in this finding:** no header fields, and a closing checklist carrying an *unchecked* box reading `- [ ] Plan-owner acceptance and acceptance date recorded`. Those two documents do not merely omit the status. They affirmatively state that acceptance has not been recorded while the ledger records it, so a reader consulting either alone would reach exactly the wrong conclusion this review reached in iteration 23. By consequence the order is: IDR-053 and IDR-054 first; then IDR-055 and IDR-056, missing header plus stale prose; then the fourteen Form A reports, contradictory but with a correct header available; then IDR-051 and IDR-052, correct information in an unusual place.

**IDR-052 also corrects a coverage record.** It is one of the eighteen reports recorded as fully read, and its missing acceptance header was never noted, because the sub-form had not been enumerated. That entry is corrected rather than left implying a clean result, as IDR-006 and IDR-014E were in iteration 29.

**A false positive, and a verified count (iteration 32).** The final research batch produced the first match that is **not** an instance. IDR-SRV-059 line 340 reads: "These recommendations are research outputs awaiting the project lead's decision, not new requirements imposed by report publication." The search matched it on "awaiting … project lead." The sentence is not stale. It says the report's *recommendations* await a decision about whether they become requirements, not that the *report* is unaccepted, and that stays true after acceptance. It is the same distinction this review applies throughout, and IDR-SRV-060 recommendation 4 states it directly: "Do not conflate research acceptance with adoption."

Because the pattern has a demonstrated false-positive mode, every stale-wording match was then read in place rather than trusted. **All sixteen are genuine**, in three shapes: eleven are direct status claims; two are header-field claims, IDR-055 and IDR-056 both carrying "**Research Result:** Complete; report awaiting Glaux Project Lead review"; one is the IDR-010A decision register marking its decisions pending; and two are a milder forward-authorization variant, IDR-045 and IDR-046, stale because the successor topics they describe as unauthorized were in fact authorized and executed. **The population stays at twenty, and its basis is now verification rather than pattern matching.** The two milder instances are distinguished from the eleven direct claims so the editorial pass can judge them separately. Evidence: [37-pass-3c-32-batch14-final-research-cluster.md](evidence/37-pass-3c-32-batch14-final-research-cluster.md), §2.

**Disposition unchanged again**, and the remedy is unchanged: the dated closing acceptance record from IDR-026 fixes both forms at once, because it supplies in the body precisely what Form B lacks in the header. The governance ledger remains correct and controlling for all twenty, which the synthesis report states at its line 120. That is true, and it is why the per-report residue matters: the ledger is right and twenty reports contradict it locally. The practical change is that the editorial pass can begin with the two reports that state the opposite of the truth. Evidence: [36-pass-3c-31-batch13-operations-fixtures-performance-interop.md](evidence/36-pass-3c-31-batch13-operations-fixtures-performance-interop.md), §2. Evidence: [35-pass-3c-30-batch12-platform-streaming-and-deployment.md](evidence/35-pass-3c-30-batch12-platform-streaming-and-deployment.md), §2.

**Third instance, and the pattern (iteration 25, IDR-010A).** IDR-SRV-010A's Appendix C Decision and Evidence Register marks all nine of its decisions "Pending plan-owner acceptance," although the final research report lists the topic as Complete and Accepted. With IDR-043 and IDR-055 that makes three. The same batch also found the correct practice in four reports: IDR-001 §8.2 records its acceptance decision in the body, and IDR-012, IDR-013 and IDR-014 each open their decision register with a dated acceptance statement and carry a per-decision acceptance column and owner. So this finding describes a recurring residue rather than an isolated slip, and a template for fixing it already exists inside the corpus. Evidence: [30-pass-3c-25-batch7-key-section-screen-b.md](evidence/30-pass-3c-25-batch7-key-section-screen-b.md), Section 4.

**Second confirmed instance, and the stronger one (iteration 24, IDR-055).** IDR-SRV-055 was accepted by the Glaux Project Lead on September 16, 2026, recorded in the overall research plan's decision log and in the final research report's topic inventory, which lists it as Complete and Accepted. The report itself does not say so. Its header omits the `Accepted By` and `Acceptance Date` fields that IDR-030, IDR-034, IDR-039A and IDR-043 all carry, and its closing block still reads "**Research Result:** Complete; report awaiting Glaux Project Lead review," with its acceptance boundary written conditionally throughout. This is more consequential than the IDR-043 instance above, where the header carried the acceptance correctly and only a trailing sentence and a checklist line were stale: here a reader consulting the report alone cannot discover that it was accepted, and the text affirmatively states the opposite. IDR-001 §8.2 shows the contrasting practice in the same corpus, recording its own acceptance decision and date in the report body. This review's iteration 23 was itself misled by the omission and recorded the report as unaccepted until the governance record was supplied, which is direct evidence of the consequence. Evidence: [29-pass-3c-24-idr055-acceptance-correction-and-batch6.md](evidence/29-pass-3c-24-idr055-acceptance-correction-and-batch6.md), Part A.

**Saved evidence:** [08-pass-3c-04.txt](evidence/08-pass-3c-04.txt), starting at line 50. Consult later qualifications below where applicable.

### F-18 - ControlStream live/admission semantics

**Status:** Narrowed behavior clarification candidate. **Reported severity:** Low.
**Affected work:** #156 / #163 / #164; relevant Feasibility tasks.

Schema annotations constrain representations but do not supply a live-derivation algorithm or every admission rule. false and null, and Command and Feasibility, need distinct consideration. The research's 409 recommendation is not automatically the only standard-compliant outcome.

**Next consideration:** State and test the selected behavior within existing owners. Do not invent a stream lifecycle, conflate Feasibility with actuation, or infer unrestricted ownership from a missing annotation.

**A remedy location (iteration 33).** Guide §8.2's two occurrences of "live" are both "live delivery" and "live-only MQTT recovery," about transport rather than the `live` field, so no scenario exercises `false` against `null`, which is the distinction this finding records. Scenario 4 is the natural home. Disposition unchanged. Evidence: [38-pass-3c-33-batch15-scenario-pass.md](evidence/38-pass-3c-33-batch15-scenario-pass.md), §4.

**Support for the narrowed disposition (iteration 28, IDR-020).** This finding records that `false` and `null` need distinct consideration. IDR-SRV-020 §17.2 asks the same question in its own words, as question 4: "When should DataStream/ControlStream `live` be `null` versus Boolean, and what evidence/age controls its derivation?" It sits among questions the report describes as "assigned downstream and do not block acceptance of the semantic baseline." An accepted report recording the same question as open and non-blocking supports keeping this finding where it is, a narrowed behavior-clarification candidate with named owners, rather than treating the Guide's silence as a defect. Evidence: [33-pass-3c-28-batch9-completion-and-batch10.md](evidence/33-pass-3c-28-batch9-completion-and-batch10.md), §1.3.

**Saved evidence:** [10-pass-3c-06.txt](evidence/10-pass-3c-06.txt), starting at line 21. Consult later qualifications below where applicable.

### F-19 - Audit modification and retention boundary

> **Confirmed twice at task level (iterations 37 and 40).** The two restore tasks written so far both omit audit. Issue #26 verifies identity, semantic fields, revisions and artifacts; issue #126 extends restore regression to streams, contracts, exact values, quality and source artifacts, event parents and deletion evidence. Deletion evidence is present in the second; audit records are absent from both. Two phases, the same shape. The design exists at Guide lines 602 and 529, and no task yet checks that audit survives a restore. Recorded, not raised; disposition unchanged. Evidence: [46-pass-3c-40-batch22-phase3-task-assessment.md](evidence/46-pass-3c-40-batch22-phase3-task-assessment.md), §4.
>
> **Remedy location (iteration 33).** Guide §8.2 contains no occurrence of `audit`, and §8.4 contains one, inside "resource/audit/outbox transaction" in the ingest row, which says audit is *written* rather than that its behavior is verified. Scenario 6 restores a backup and verifies "resource meaning, identity, deletion handling, and held command work" without mentioning audit. The design exists, at line 602 for the audit floor and 529 for restore testing; scenario 6 is where the check belongs. Disposition unchanged. Evidence: [38-pass-3c-33-batch15-scenario-pass.md](evidence/38-pass-3c-33-batch15-scenario-pass.md), §4.

**Status:** Clarification candidate. **Reported severity:** Low-Medium.
**Affected work:** #15; retention/configuration owners.

The explicit Guide floor covers durable transactional accountability, not a complete tamper-evidence architecture. The reviewer withdrew its INSERT-implies-UPDATE/DELETE premise. The remaining question is intended audit protection and retention behavior.

**Next consideration:** Separate serving access from owner/migration and authorized retention roles. Append-only recommendations must account for permitted disposition; hash chains are not automatically required.

**Related instance (iteration 20, restore versus deletion).** IDR-SRV-030 requires that a restored older point receive the later deletion and tombstone state before it can serve data (recommendation 11, Section 13.1), and rejects restoring a backup directly into service because it "Can resurrect deleted/unauthorized state" (Section 18.2). Guide Section 4.7 treats restore carefully and enumerates several things it does **not** re-establish: a rollback or fork forces a fresh recovery epoch and pre-restore cursors fail with `410` (lines 531, 547), restored pending work stays held, command reconciliation covers commands accepted after the backup, and same-key deduplication is explicitly not promised across a lost recovery interval (line 531). Deletion is absent from that list. Restoring to a point before a deletion returns the resource to the authoritative store, and because tombstones live in that same store they roll back with it, while reads are the part the Guide re-enables first (line 529). A peer that already received the deletion is protected, because an import that would resurrect a deleted resource is retained and reported rather than applied (line 647), so the exposure is local reads and any re-export from the restored node. Evidence: [25-pass-3c-20-idr030-and-queue-corrections.md](evidence/25-pass-3c-20-idr030-and-queue-corrections.md), Section 5.3.

**Sharpened (iteration 21, after finishing the IDR-030 sections iteration 20 had only skimmed).** The earlier statement hedged that this is partly inherent to point-in-time restore because there may be nothing to reapply. IDR-030 Section 12 shows the research does not stop at naming the hazard. The archive manifest is specified to carry "tombstone/deletion-ledger bounds" so a gap is detectable (Section 12.1); the restore contract is an explicit sequence whose step 2 restores "into an isolated, non-serving namespace" and whose step 6 reads "Apply the current tombstone/deletion ledger and every later disposition event before publication. Never expose restored pre-deletion state", with a "deletion-ledger gap" named as a terminal failed or review state (Section 12.2); and Section 6.2 lists the tombstone ledger among the restore gates. The Guide adopts none of that archive machinery, so the manifest mechanism is not available to it, but the ordering principle of restoring into isolation and reconciling deletions before serving is available and cheap to state. Section 16 supplies two ready-made fixtures: "Valid restore | Restore prior backup containing later-deleted record | Current deletion ledger reapplied before serving; deleted resource does not reappear" and "PITR replay | Restore point precedes deletion | Post-restore deletion/tombstone replay removes it before access". Evidence: [26-pass-3c-21-idr030-completion-and-idr034.md](evidence/26-pass-3c-21-idr030-completion-and-idr034.md), Sections 2.1-2.2.

**Next consideration for that instance:** State in Section 4.7 what a restore does to deletions recorded after the backup point, and whether authorized reads are gated until that is reconciled or the limitation is simply documented as the other restore limitations are. Add the corresponding check to the Section 8 restore verification, which already exercises tombstones during restore testing. Documentation and test changes within existing owners; no new capability is implied.

**Related instance (iteration 16, IDR-038 audit invariants):** IDR-SRV-038 Appendix B.2 invariant 10 ("No correction, reconciliation, retention, redaction, or export rewrites original audit evidence") and Appendix A's audit-search/export/redaction row ("derived redaction never alters original") sharpen this finding's open question into a specific candidate rule. Guide v1.3 line 602 states a durable-record floor (verified actor/source, operation, target/revision, time, outcome, correlation identifier) and explicitly disclaims one specific architecture ("A mandatory tamper-proof ledger ... is not proposed") without stating whether correction, redaction or export may alter the original evidence versus only a derived view. The candidate rule is not itself adopted by that floor. No Guide correction; no new finding. Evidence: [21-pass-3c-16-research-deep-read-slice1.md](evidence/21-pass-3c-16-research-deep-read-slice1.md), Section 5.

**Saved evidence:** [15-pass-3c-11.txt](evidence/15-pass-3c-11.txt), starting at line 55. Consult later qualifications below where applicable.

### F-20 - Denial-audit deliverable

**Status:** Clarification/test candidate. **Reported severity:** Low.
**Affected work:** #15 / #22.

The Guide requires relevant denials to be recorded safely; the reviewed issue text allows safe denial auditing but does not explicitly test selected record creation/failure behavior. Existing owners are present. Proposed event categories are suggestions.

**Next consideration:** Define selected categories, bounds and failure behavior if adopted. Audit failure must never authorize the denied action; no independent spool follows automatically.

**Related instance (iteration 16, IDR-038 audit event catalog):** IDR-SRV-038 Appendix B.1 names a 37-item minimum audit-event catalog (`request_received` through `audit_integrity_incident`). Guide v1.3 line 602 states only a field-level floor (verified actor/source, operation, target/revision, time, outcome, correlation identifier), not a named-event taxonomy. The catalog is a concrete candidate for the "selected categories" this finding already asks the owner to define; it is not itself an adopted requirement. No Guide correction; no new finding. Evidence: [21-pass-3c-16-research-deep-read-slice1.md](evidence/21-pass-3c-16-research-deep-read-slice1.md), Section 5.

**Saved evidence:** [15-pass-3c-11.txt](evidence/15-pass-3c-11.txt), starting at line 56. Consult later qualifications below where applicable.

### F-21 - Node-local versus exchanged audit history

**Status:** Documentation clarification candidate. **Reported severity:** Low.
**Affected work:** #240 / exchange documentation.

The described exchange format is not a replication protocol for the complete authenticated audit trail. Supplied production context may still travel with resources. Full cross-node audit durability was not promised.

**Next consideration:** Clarify the boundary if useful. Backup protects its captured recovery point, not every later event. Do not automatically add audit synchronization.

**Saved evidence:** [15-pass-3c-11.txt](evidence/15-pass-3c-11.txt), starting at line 74. Consult later qualifications below where applicable.

### F-22 - Exporter-side accountability

**Status:** Proposed strengthening / credible risk. **Reported severity:** Medium.
**Affected work:** #240 / Guide audit floor.

No explicit exporter-side durable audit requirement was found in the checked Guide and export issue. Import receipts are not proof of an exporter-side record. Implementation has not begun, so this is not proof of an observed missing runtime record.

**Next consideration:** Consider scoped export auditing. First distinguish generation, release/handoff and confirmed receipt; protect actor/recipient/scope metadata and define failure/partial-output behavior. No delivery-receipt platform is implied.

**Saved evidence:** [15-pass-3c-11.txt](evidence/15-pass-3c-11.txt), starting at line 81. Consult later qualifications below where applicable.

## Resolved standards checks (no finding)

These carried standards questions were closed with evidence and produced no new finding. They are recorded here so a successor does not redo them; the structured status lives in `remaining_checks` of [review-state.json](review-state.json).

| Check | Disposition | Where verified |
|---|---|---|
| SWE Common 3.0 `quality` schema/semantic claim (Guide Section 4.3 and Section 13 row) | **Supported.** 24-014 Clause 8.2.3 declares an optional multi-valued `quality` attribute of the Clause 8.2.15 union; the published `AbstractSimpleComponent.json` declares no `quality` member and no schema sets `additionalProperties`/`unevaluatedProperties: false`, so the member is schema-permitted but unvalidated. The Guide's array-of-components reading follows the model; its local `href` reference for dynamic quality is a labelled Glaux choice. No Guide correction. | [17-pass-3c-12-standards-swe.md](evidence/17-pass-3c-12-standards-swe.md), Section 2 |
| `recordsAsArrays`/`vectorsAsArrays` "true means arrays" (Guide Section 13 row; register #71) | **Supported; register #71 confirmed verbatim.** Requirement 85A/86A literally reverse the booleans and contradict their own default sentence; Clause 8.7.1, the 10.2.3 introduction, `encodings.json` and Annex B.2.4 all make `true` mean arrays. Consequence added under F-08: A.85/A.86 test only the object form. Optional one-clause clarification to the Guide row; no upstream filing authorized. | [17-pass-3c-12-standards-swe.md](evidence/17-pass-3c-12-standards-swe.md), Section 3 |
| Features Part 3/CQL2 class identifiers, dependencies, `cql2-json`-only default and GeometryCollection `minItems` (Guide Section 7.4, Section 6.3.1 line 799, Section 13 row 1162) | **Supported.** All six `/conf/` URIs exist verbatim in 19-079r2 and 21-065r2; the dependency prose matches Clauses 6.1/7.5/7.6/8.3 and 19-079r2 Clause 8.1/Annex A.1. Listing only `cql2-json` and making it the default is conformant under 19-079r2 Requirement 6 B/C (no language is mandatory; the text/JSON SHOULDs belong to the unselected Features Filter class). Annex C.1 and the published `cql2.json` set `geometrycollection.geometries` `minItems: 2` while Clause 7.6.1 prose and the Annex B BNF allow one or more, exactly as the Guide row states; singleton acceptance exceeds the Requirement 33/38 schema-validity floor and is untested by the official suite, so it remains a labelled Glaux adaptation. Test-evidence notes recorded under F-08. No Guide correction. | [18-pass-3c-13-standards-cql2-sensorml.md](evidence/18-pass-3c-13-standards-cql2-sensorml.md), Section 2 |
| SensorML 3.0 class identifiers `json-simple-process`, `json-physical-system`, `json-deployment`, `json-derived-property` (Guide Section 1.2 line 255) | **Supported.** OGC 23-000 (published 2025-07-16) defines all four under `http://www.opengis.net/spec/sensorML/3.0` with conformance classes A.12, A.15, A.16, A.17 (Requirements 47, 50, 51, 52, each schema validation against the published JSON schema). Published prerequisites: `json-physical-system` requires `json-aggregate-process` (A.13) and `json-physical-component` (A.14); all four rest on `json-core` (A.11; Requirement 46 media type `application/sml+json`, as the Guide uses). Optional clarification: name A.11/A.13/A.14 explicitly beside the four selected classes so the claim manifest cannot omit them. No Guide correction. | [18-pass-3c-13-standards-cql2-sensorml.md](evidence/18-pass-3c-13-standards-cql2-sensorml.md), Section 3 |
| Remaining IDR-011 Section 14.3 abstract-test discrepancy rows (nine of thirteen; Guide Section 7.2 adapted-test rule, Section 13 singular-path and System Event rows) | **Confirmed; all thirteen rows now verified against the published text.** 23-001 A.65 `indirect-prop` repeats `/systems` in its sampling-feature step (recommendation test, warning-only); 23-002 A.61 iterates Commands and reads `currentStatus` where Requirement 61 B filters CommandStatus by `statusCode`; A.35 iterates `itemType` Command for Feasibility; A.36 tests `/controlstreams/{dsId}/commands` for `/controlstreams/{csId}/feasibility`; A.40/A.42 use ControlStream targets for System Events; A.43 uses `/systems/{sysId}/systemEvents` against normative `/systems/{sysId}/events`; A.62 lowercases `/systemevents` and reads conceptual `type` (JSON `definition`); A.50 omits Requirement 50 D `latest`; A.31/A.33 never exercise Requirement 31 B/33 B `limit`/`datetime`. IDR-011's corrections stand; four further published seams (Requirement 61 A description, Requirement 31 B clause cite, A.66 label, vacuous Part 2 canonical-URL tests) are recorded as overlay candidates. No Guide correction. | [19-pass-3c-14-standards-ats-rows-part2-inheritance.md](evidence/19-pass-3c-14-standards-ats-rows-part2-inheritance.md), Sections 2 and 4 |
| Part 2 Annex A.1 `/conf/api-common` inheritance (Guide Section 1.2 line 262 bases; Section 7.1 `api-common` rows and closing paragraph) | **Supported.** 23-002 Clause 8 and Annex A.1 both inherit Part 1 `api-common`; every Part 2 resource class lists Part 2 `/conf/api-common`; the Part 1 Annex prerequisite conflict (F-08) therefore propagates to every Part 2 declaration, and no additional inherited class is introduced. Research already records the chain (IDR-008 Sections 7.2-7.3, IDR-007 P2-INT-008, IDR-006 INT-CS1-001). Part 2 A.2 is conditional on exposed non-feature collections. Related instance recorded under F-08; no Guide correction beyond wording the requested F-08 row for both Parts. | [19-pass-3c-14-standards-ats-rows-part2-inheritance.md](evidence/19-pass-3c-14-standards-ats-rows-part2-inheritance.md), Section 3 |

Material follow-up questions raised by the same reads (encodings.json root `oneOf` omitting `BinaryEncoding`; `Quantity.json` requiring `label`; the CQL2 Clause 7.6.1 GeometryCollection member list omitting LineString; the 23-000 Clause 9.1.2.1 draft media-type note; whether the Glaux ATS overlay absorbs the four additional published seams; whether Glaux exposes any Part 2 resource collections; the wording of the F-08 Section 13 row for both Parts; whether one unknown-member rule should cover ordinary-JSON Part 2 resources, GeoJSON `properties` foreign members and the SensorML `extension` slot together; whether Guide Section 4.12 should state metrics/traces exposure is internal/access-restricted by default; which CS-Go repository URL is the correct pinned source; whether the approved Part 2 baseline actually prohibits a future `resultTime`; whether the Guide should cite IDR-055 for its security-test content; whether the Guide should state `Command.issueTime` semantics and address `systemType`/`systemKind`) are recorded in `follow_up_questions` of the state file. They are not review obligations unless a later batch selects them.

The F-13 controlling extension-rule check (`observation-extension-policy`, iteration 15) was also closed in the state file; because it re-dispositions an existing finding rather than producing none, its record is the F-13 entry above and [20-pass-3c-15-observation-extension-policy.md](evidence/20-pass-3c-15-observation-extension-policy.md). No new finding number was created.

**Research deep-read remainders, slice 1 (iteration 16):** IDR-008 Sections 16-19, IDR-037 Appendices A-D, IDR-038 Appendices A-C, and IDR-039 Section 7 and Sections 21-22 were read in full and accounted against Guide v1.3. No Guide correction and no new finding number resulted; three related instances were recorded above (F-03, F-19, F-20) and one new follow-up question was recorded (`fq-09`, metrics/traces exposure). All four reports move from partial to fully read. Evidence: [21-pass-3c-16-research-deep-read-slice1.md](evidence/21-pass-3c-16-research-deep-read-slice1.md).

**Research deep-read remainders, slice 2 (iteration 17):** IDR-040 Sections 9-21 were read in full and accounted against Guide v1.3, with RFC 9111 Sections 3.5, 4.1 and 5.2.2.7 retrieved to bound the one gap statement. The report's invariants are substantially adopted in the Guide's own vocabulary; its typed policy machinery (`PolicyAssertion`/`PolicyBinding`/`DisclosureDecision`/`DerivedViewProvenance`, `INDETERMINATE` three-state decisions, transform registry, bilateral federation mappings, per-view OpenAPI derivatives) is not adopted, recorded as a scope choice consistent with this register's F-03 disposition and with the report's own Section 18.2 deferrals. One evidenced gap was found and recorded as the F-03 related instance above: no Guide rule binds a response cache validator or cache key to the authorized view. No new finding number. One new follow-up question was recorded (`fq-10`, the CS-Go repository URL discrepancy, deliberately unverified). IDR-040 moves from partial to fully read. Evidence: [22-pass-3c-17-research-deep-read-slice2.md](evidence/22-pass-3c-17-research-deep-read-slice2.md).

**Research deep-read remainders, slice 3 (iteration 18):** IDR-042's unread sections (3-4, 6, 7, 9, 10, 15, 16, 19-20) were read in full and accounted against Guide v1.3. Its non-collapse rules, freshness and last-known vocabulary, `resultTime=latest` semantics, delayed and replayed input handling, opaque policy-bound cursors, MQTT-session-is-not-a-cursor rule and reconnect/resnapshot behavior are substantially adopted, several passages near-verbatim. `RepresentationAssessmentV1`, the Glaux-specific time fields, the DDIL mode taxonomy and numeric thresholds are not adopted, a recorded scope choice consistent with the report's own Section 3.2 and Section 19. The planned re-test of the F-03 response-cache gap against this report found it **reinforced**: a second accepted report states the missing rule and adds a cache-versus-domain-freshness consequence with two fixtures, recorded in the F-03 instance above. No new finding number and no new follow-up question. IDR-042 moves from partial to fully read. Evidence: [23-pass-3c-18-research-deep-read-slice3.md](evidence/23-pass-3c-18-research-deep-read-slice3.md).

**Research deep-read remainders, slice 4 (iteration 19):** IDR-043's unread sections (Sections 5-14, 16-18, 21-22 and the checklist) were read in full and accounted against Guide v1.3. Its non-bypass invariant, the prohibition on a generic synchronization route, identity and revision rules, tombstone and anti-resurrection handling, inbox idempotency, the replay classification table, the prohibited universal conflict resolvers, staged-conflict isolation with an explicit resolution allowlist, federation partner-profile requirements, command non-redispatch and the observability rules are substantially adopted, several near-verbatim; Guide line 633 cites the report directly. Its `SyncEnvelopeV1` field taxonomy, five orthogonal state machines, `SyncConflictV1`, quarantine subsystem, operator review workflow and versioned federation profiles are not adopted, a recorded scope choice stated at Guide line 620. Its Section 16.1 `428` row belongs to the already-recorded no-mandatory-precondition choice. One finding instance was confirmed under F-17 above. No new finding number and no new follow-up question. IDR-043 moves from partial to fully read. Evidence: [24-pass-3c-19-research-slice4-and-batch-queue.md](evidence/24-pass-3c-19-research-slice4-and-batch-queue.md).

**Committed deep read IDR-030 (iteration 20, queue batch 2):** read in full and accounted against Guide v1.3. Its central recommendations are adopted at Guide line 527, which forbids automatic retention or purge by default and permits removal only under an explicitly configured policy that preserves visible deletion behaviour and dependencies among values, schemas, source documents, pending work and synchronization tombstones. Also adopted: the cascade and `409` rules with retained internal evidence, no reuse of a deleted identifier, cancellation as a status rather than a deletion, idempotency-key retention that outlives replay horizons, rebuildable derived state, and the prohibition on fabricating history removed by retention. Its archive tier, hold overlay, policy registry, disposition receipts, purge automation, cryptographic erase, media sanitization, five orthogonal lifecycle state dimensions and aggregation rules are not adopted, a recorded scope choice the report supports by assigning those to deployment, security and profile authorities and by supplying no numeric horizon of its own. One documentation gap was found and recorded as a related instance under F-19 above. Evidence: [25-pass-3c-20-idr030-and-queue-corrections.md](evidence/25-pass-3c-20-idr030-and-queue-corrections.md).

**Committed deep read IDR-034 (iteration 21, queue batch 3):** all nineteen sections plus the checklist read in full and accounted against Guide v1.3. Substantially adopted across latest-value selection and ties, schema evolution with successor streams, `live` semantics, the optional/nil/uncertain/withheld distinctions, authorization ordering before every derivation, the PostgreSQL-first storage posture with TimescaleDB conditional, publication after commit, and System Event separation. It confirmed the F-14(c) instance above, supplied a third source for the F-03 cache instance, and its request for snapshot-stable paging is a departure the Guide states for itself at lines 444, 471 and 473, pointing to the explicit snapshot and export mechanism instead. One incidental uncertainty was recorded as `fq-11` rather than asserted: the report says a future `resultTime` is prohibited and the Guide has no such rule, but its own footnote attributes the cited requirements to parent schemas and UTC time rather than to futurity, so the obligation was not verified. Evidence: [26-pass-3c-21-idr030-completion-and-idr034.md](evidence/26-pass-3c-21-idr030-completion-and-idr034.md).

**Committed deep read IDR-039A (iteration 22, queue batch 4):** all twenty-five sections plus the evidence legend read in full. The cross-check the queue required is confirmed: IDR-040 Section 14.1 and IDR-042 Section 14.1 both cite this report for signed versioned anti-rollback policy and trust bundles, a bounded local authorization envelope, disconnection never expanding authority, and an indeterminate result on absent or stale mandatory inputs, and every one of those elements exists in the source saying what the citing reports say, so no correction is needed in any of the three. The Guide contains none of the zero-trust vocabulary and cites the report once at line 592; that absence is compliance with the report's own rule that no enterprise zero-trust, maturity or accreditation claim may be made without a competent external authority, not an omission. The enforcement substance is substantially adopted across authorization ordering, authorized-view artifacts, fail-closed behaviour, anonymous handling, proxy trust, streaming, commands, source separation and telemetry redaction. Its named logical architecture is not adopted, a recorded scope choice in this register's F-03 family that the report itself supports through its own deferrals. No new finding and no new follow-up question. Evidence: [27-pass-3c-22-idr039a.md](evidence/27-pass-3c-22-idr039a.md).

**Committed deep read IDR-055 (iteration 23, queue batch 5):** all nineteen sections plus the legend, contents, header and closing block read in full. **Corrected in iteration 24:** this report **is accepted**, by the Glaux Project Lead on September 16, 2026. Iteration 23 read only the report's own metadata, where the header carries no acceptance field and the closing block still states that it awaits project-lead review, and wrongly concluded it was unaccepted. Because the report is accepted while its own text says otherwise, this is a second confirmed instance of F-17, recorded above. The handoff from IDR-039A is consistent on all four handed-over elements, with the required fourteen-column matrix delivered as a test-plan matrix in its own columns rather than a copy of the enforcement columns. The security-test substance is substantially adopted, including the paired-world non-interference method which the Guide already holds at line 975 and the Guide's own security-test family list at line 612. Its tool portfolio, execution tiers, evidence schema, twelve proofs and simulator harness are not adopted and are correctly not recorded as gaps: the standing instruction forbids adding a tool portfolio because an optional mechanism is absent, and F-02 already owns the enforced-CI question at the right altitude. Iteration 23 gave a third reason, that the report was unaccepted; that reason is withdrawn and the conclusion stands on the remaining two. Evidence: [28-pass-3c-23-idr055.md](evidence/28-pass-3c-23-idr055.md).

**All four committed deep reads are complete.** The research deep-read check is closed.

**Key-section screen A (iteration 24, queue batch 6):** the seven standards and obligation baseline reports were screened at their decision-usable sections. IDR-001 to IDR-005 were screened at their recommendation and risk sections; IDR-006 and IDR-007 at their recommendation sections, with their already-read §13 risk registers reused rather than re-read. None is recorded as fully read, and each state entry lists exactly which sections were screened. Their recommendations are substantially adopted, several near-verbatim: the rule against forking the upstream example OpenAPI (Guide line 362), opaque `next` links rather than a promised offset API (444), and SSE as a Glaux interface rather than a Part 3 binding (547). No recommendation contradicts the Guide and none produced a new finding. Two candidates were recorded without assertion, since a screen cannot establish them: `fq-13` asks whether the Guide should state `Command.issueTime` semantics and address `systemType`/`systemKind`, neither of which appears in it, and an A.115 binary/text candidate was added to the existing `fq-05` rather than opening a new question. Evidence: [29-pass-3c-24-idr055-acceptance-correction-and-batch6.md](evidence/29-pass-3c-24-idr055-acceptance-correction-and-batch6.md), Part B.

**Key-section screen B (iteration 25, queue batch 7):** the six accepted conformance, API definition and documentation reports were screened at their decision registers and equivalent sections, with IDR-010's link sections reused rather than re-read. None is recorded as fully read. Their decisions are substantially adopted across OpenAPI generation, conformance declaration, URL stability, status-code boundaries and error semantics, several near-verbatim. Three results are recorded under existing findings above: the F-03 cache instance's change of character, the F-11 confirmation, and the third F-17 instance. The batch also recorded three non-adoptions the Guide states for itself, which illustrate the standing distinction between accepted research and mechanisms adopted into the plan: the deprecation calendar duration is declined at Guide line 857, the documentation renderer is deferred at 362, and the capability-registry framing is narrowed to "ordinary shared metadata, not a new registry service" at 360. One cross-report reconciliation was recorded: IDR-014's accepted decision against per-user OpenAPI documents supports the Guide's single-document choice that iteration 17 had recorded as not adopting an earlier report's per-profile proposal. Evidence: [30-pass-3c-25-batch7-key-section-screen-b.md](evidence/30-pass-3c-25-batch7-key-section-screen-b.md).

**Key-section screen C and the peer-source checks (iteration 26, queue batch 8):** the eight peer and client implementation studies were screened at their recommendation and risk sections, with IDR-014B's prior references reused. None is recorded as fully read. Their recommendations are substantially adopted, several near-verbatim: no reliance on startup auto-migration (Guide line 529), cursors bound to caller, scope, policy version and expiry (547), typed configuration with unknown-key rejection and redacted diagnostics (663), discriminating seeded data rather than status-only criteria (446), the outbox seam that prevents a committed mutation losing its event (119, 539), configuration-true generated discovery rather than a wildcard as an authorization promise (560), and delivery claims kept separate from broker quality of service (564).

The same batch ran the outstanding peer-source spot checks, since they needed the same sources open. All four produced definite results. Both recorded pins verify exactly, and the OpenSensorHub result independently corroborates a study claim that its tag is lightweight. The Guide's own peer-source citation verifies in full: the commit exists, the latest-observation test does assert the expected identifier, the adjacent time-range test does assert only the item count and never which item, and the Guide's qualification that this is source analysis rather than an observed defect is also correct. One incidental verification was recorded and then **withdrawn in iteration 27**: that same test asserts exactly one item for the latest selector, and iteration 26 wrote that this corroborates two research claims about the peer's `latest` selector. It cannot. The fixture seeds a single newest observation, so it never exercises tied result times and nothing in it distinguishes a correct implementation from an incorrect one. What the test records is its author's expectation, not the server's behavior. The time-range observation beside it is independently supported and stands: the adjacent test asserts only the item count and never which item, so it would pass if the wrong observation were returned, which is exactly what Guide line 446 forbids. Guide line 440 was never at issue and is unaffected, and no finding rested on the withdrawn claim. The **peer-source-spot-checks** check is closed on the verification results, which are unaffected, with no new whole-history study performed. Evidence: [31-pass-3c-26-batch8-peer-studies-and-source-checks.md](evidence/31-pass-3c-26-batch8-peer-studies-and-source-checks.md), corrected in its Appendix A and in [32-pass-3c-27-batch8-corrections-and-batch9.md](evidence/32-pass-3c-27-batch8-corrections-and-batch9.md), Section 1.

**Completing screen C (iteration 27, Correction B):** iteration 26 stopped at the first subsection of each risks-and-open-questions section and left the Open Questions subsections unread. All 102 omitted lines in IDR-014C, IDR-014D, IDR-014E, IDR-014F and IDR-014G have now been read. Nothing in them changes the iteration 26 accounting. The questions are overwhelmingly about the peer projects and the studies' own method rather than about Glaux: all ten of IDR-014D's remaining questions ask what the SECD prototype intends, and IDR-014E, IDR-014F and IDR-014G each state that no open question blocks their report or escalates to Glaux. Three items were worth keeping, all recorded under existing findings above: the first peer-study support for F-12, the IDR-014G compatibility-adapter question under F-11, and two IDR-014E risk rows that turn out to be adopted already at Guide lines 991 and 944. Evidence: [32-pass-3c-27-batch8-corrections-and-batch9.md](evidence/32-pass-3c-27-batch8-corrections-and-batch9.md), Section 2.

**Key-section screen D (iteration 27, queue batch 9):** the six reports covering the canonical resource model, identifiers and lifecycle, relationships, temporal validity, provenance and status were screened at their decision-usable sections, with IDR-017's §9.1 reused rather than re-read. None is recorded as fully read. Acceptance was checked in the governance record rather than inferred from report metadata, and all six record it correctly, which is the bounding negative result recorded under F-17. Their recommendations are substantially adopted and several are near-verbatim: never reuse a deleted ID (Guide 509), cancellation is a status transition and not HTTP DELETE (582), a new stream ID for incompatible populated schema replacement (408 and 434), no universal readiness score (483), System Events only for genuine operational occurrences (485), no universal trust score (602), and quality assertions preserved without normalizing them into a generic truth probability (608).

Two non-adoptions in that batch illustrate the standing distinction between accepted research and mechanisms adopted into the plan, and neither is a defect. IDR-018's R-018-03 asks for snapshot or watermark paging; Guide line 444 documents keyset paging as a changing view instead, a departure the Guide states for itself, while adopting the cursor-binding half at 471. And IDR-019's central mechanism, a PROV-compatible evidence graph, is declined three times on the record: at Guide line 282, at Guide line 1118, and in the governance record itself, which states that the "PROV-compatible evidence graph" wording is not a mandatory graph database or public PROV service and that the Guide's practical records remain the starting point. The obligations IDR-019 derives are adopted; only the architecture is not.

The batch's material result is recorded under F-11 above: IDR-017's R-017-08 states the relation-spelling rule that finding asks for. Evidence: [32-pass-3c-27-batch8-corrections-and-batch9.md](evidence/32-pass-3c-27-batch8-corrections-and-batch9.md), Section 3.

**Completing screen D (iteration 28):** batch 9 had been recorded as done while the risks-and-open-questions sections of four of its six reports were unread, which its saved scope assigns. The batch was reset to partial before anything was read, and the 157 omitted lines in IDR-016, IDR-017, IDR-018 and IDR-020 were then finished. Four results are recorded under existing findings above: F-03's named owners, the F-11 correction, F-18's support, and F-17's widened bound. One observation was recorded and deliberately not raised: IDR-016 lists UUIDv7 timestamp visibility as a privacy risk and routes it to IDR-039/040, while Guide line 372 adopts UUIDv7 with an opacity rule that addresses the time-authority half of the risk and not the privacy half. One consequence was added to a departure already accounted: IDR-018 states that a re-evaluated `now` across pages causes duplicates, omissions and inconsistent state, which is what the Guide's changing-view paging at line 444 accepts and documents rather than prevents.

The coverage comparison then required before marking the batch complete found a further omission the four named sections did not cover: IDR-015's §§15 and 15.1, a decision register. It was read. Option D, typed encoding-neutral aggregates with codecs, is adopted, and an event-sourced model as mandatory architecture is deferred rather than required, both matching the Guide. Batch 9 is complete on that basis, and the section-by-section comparison is now a published stop condition for every remaining screen batch rather than an intention. Evidence: [33-pass-3c-28-batch9-completion-and-batch10.md](evidence/33-pass-3c-28-batch9-completion-and-batch10.md), §1.

**Key-section screen E (iteration 28, queue batch 10):** the five reports covering SensorML representation, SWE Common data components, schema and encoding validation, units and semantic binding, and database and persistence architecture were screened at their recommendations, acceptance-decision subsections and full risks-and-open-questions sections. None is recorded as fully read. All five are accepted, all five record it correctly, and all five are cited in the Guide, which is a clean contrast with the three uncited accepted reports recorded in iteration 27.

This is the most closely adopted batch the review has screened. Near-verbatim matches include PostgreSQL and PostGIS as the single authoritative store with a relational, JSONB and artifact hybrid (Guide 517), TimescaleDB benchmarked but not required (519), explicit numbered migrations with no ungoverned startup mutation (529), uncontrolled `$ref` and network resolution disabled behind a reference-closed package (420), unmodified upstream schemas with a separate documented semantic check (410), values bound to an immutable contract fingerprint that cannot reinterpret history (408), generated policy-aware SensorML with raw bytes never served as a shortcut around access checks (406), and no implicit unit conversion on command input with labels and matching dimensions not establishing property identity (424). The batch's material result is recorded under F-03 above. Two non-adoptions are recorded and neither is a defect: the quarantine and promotion lane that three reports recommend is declined by the Guide's own scope choice at line 422, where legacy import is not added as a separate implementation objective, and QUDT is an explicitly optional Medium recommendation. Evidence: [33-pass-3c-28-batch9-completion-and-batch10.md](evidence/33-pass-3c-28-batch9-completion-and-batch10.md), §2.

**Key-section screen F (iteration 29, queue batch 11):** the five reports covering geospatial storage and query, time-series observation storage, metadata and document storage, and the publisher and simulator contract boundaries were screened at their recommendations, their full risks-and-open-questions sections and their decision registers. The coverage comparison was built from the section headings before reading rather than reconstructed afterwards, which is the practice the previous two iterations had to be corrected into. None is recorded as fully read.

Adoption is close. Guide line 442 alone matches five IDR-026 recommendations near-verbatim, on CRS84 GeoJSON order, `bbox` and `geom` as distinct contracts including geometry-less features, fixtures for antimeridian crossing and 3D and invalid coordinates, and never silently discarding vertical or moving-position information. Guide line 505 matches IDR-032's retry-identity recommendation almost word for word, down to the same key with a different intent being a conflict.

Two non-adoptions are stated by the Guide itself and neither is a defect. IDR-032 proposes four private publisher paths and IDR-033 a narrow simulator control plane; Guide line 1118 declines both by name. Both reports already reject the broad versions of each, so the research and the Guide agree on the principle and differ only on whether the narrow form is built. The batch's two substantive results, three new F-17 instances with the withdrawn bound, and four further F-03 sources including the rule stated in this review's own terms, are recorded under those findings above. Evidence: [34-pass-3c-29-batch11-storage-query-and-write-boundaries.md](evidence/34-pass-3c-29-batch11-storage-query-and-write-boundaries.md).

**Key-section screen G (iteration 30, queue batch 12):** the six reports covering streaming and event publication, the Rust language and framework strategy, service architecture, reference deployment, configuration and secrets, and observability were screened at their recommendations, full risks-and-open-questions sections and decision registers. IDR-044's already-recorded section was reused rather than re-read from scratch, and the report moved from partial to screened, which leaves **no partial research report outstanding**. None is recorded as fully read.

This batch contains the clearest adoption case in the review. Where earlier batches showed the Guide following a report's direction, IDR-035 shows it carrying the artifacts: the experimental profile name `glaux-csapi-part3-exp/0.1` at Guide line 549, the pinned upstream draft commit at line 268, and all five of the report's listed profile deviations, down to the deliberate lowercase `parentid` spelling that corrects the draft, which the Guide also records as a source-contradiction row. It is worth recording as a counterexample to any reading that the Guide treats accepted research loosely.

Adoption elsewhere is close. Guide line 663 alone carries three configuration recommendations, on a typed schema with unknown-key rejection, rejecting unsafe combinations, and a redacted effective manifest. Guide lines 665 and 667 carry four observability recommendations near-verbatim, including avoiding resource IDs as metric labels. Three non-adoptions are stated by the Guide for itself and none is a defect: the architecture report's package skeleton is declined by name at line 1118, the configuration report's eleven named runtime profiles are absent because the Guide configures profiles implicitly, and two accepted reports name different configuration crates, a research disagreement the Guide declines to settle in a design document. The batch's F-17 result is recorded under that finding above, and `fq-09` gained a second accepted source. Evidence: [35-pass-3c-30-batch12-platform-streaming-and-deployment.md](evidence/35-pass-3c-30-batch12-platform-streaming-and-deployment.md).

**Key-section screen H (iteration 31, queue batch 13):** the five reports covering migration and recovery, requirement-to-test traceability, fixtures and scenario corpus, performance and load, and external client interoperability were screened at their recommendations, full risks-and-open-questions sections and decision registers. None is recorded as fully read. The batch's F-17 result is recorded under that finding above, and F-12 gained two more sources.

Adoption is close. Guide lines 529 and 531 carry the recovery recommendations near-verbatim, including migrations run only through an explicit administrative command, restore into a fresh isolated target activated through a fenced cutover, restored pending work held rather than resubmitted, and a fresh recovery epoch that invalidates old continuity tokens. Guide 955 carries the fixture rules on goldens and snapshot acceptance, Guide 989 the performance rules on correctness-gated evidence and not claiming scale from a microbenchmark, and Guide 991 the interoperability rule that a client workaround is not permission to change the contract.

Two large mechanisms are stated non-adoptions and neither is a defect. The requirement-to-verification traceability graph is declined at Guide line 1118 on two counts, as a graph or evidence database and as a separate requirements set, which is the same shape as the provenance graph declined in batch 9; its derived obligations are adopted, including never converting a waived failure into a conformance pass and keeping the oracle independent of the service under test. The performance report's pinned tool selections are not named, and the Guide defers tool pinning to implementation as it does elsewhere. One earlier record was corroborated more cleanly than the review had managed: Guide line 297 names the architecture report directly while declining its package set, giving the reason. Evidence: [36-pass-3c-31-batch13-operations-fixtures-performance-interop.md](evidence/36-pass-3c-31-batch13-operations-fixtures-performance-interop.md).

**Key-section screen I, and the close (iteration 32, queue batch 14):** the five addendum studies covering draft Part 4 sampling features, enhanced querying, Part 5 Protobuf, provenance interoperability and the peer engineering-practices study were screened at their recommendations, risks and open questions, and decision registers. All five are accepted through the addendum workflow with correct headers, and all five are cited in the Guide.

One adoption is worth stating plainly. The Part 4 study offers a low-effort option marked "Recommended now" and an experimental option it explicitly does not select, which carries three preconditions if chosen. The project chose the harder path and the Guide discharged all three: line 272 names the selected types and then excludes Solid, Specimen, StatisticalSample, FeaturePart, relative and parametric types, mobile snapshot behavior and derived-volume computation by name, and pins the draft commit; lines 390-394 give the exact alias table and forbid arbitrary prefixes; line 237 requires typed round trips and query results. The querying study is adopted near-verbatim too, including the discriminator that separates a direct sampling-point match from a recursive ancestor match at line 975, and the decision to advertise only `cql2-json` at line 799.

Two earlier conclusions were independently confirmed. The provenance interoperability study, which is how the Guide reaches the provenance model report, reaches the same non-adoption on its own: its decision analysis marks a universal graph and confidence subsystem "Not recommended" and its recommendation 5 defers public PROV profiles. And the peer engineering study records that no runtime was available to it and no installations or test runs occurred, which confirms the basis on which iteration 26 closed the peer-source checks. Evidence: [37-pass-3c-32-batch14-final-research-cluster.md](evidence/37-pass-3c-32-batch14-final-research-cluster.md).

**The research area is complete.** All nine clusters are done and the sweep is closed.

**End-to-end scenario pass (iteration 33, queue batch 15):** the approved workflows and the consequential failure, security and provenance boundaries were accounted against Goal §5, Guide §8.2's nine representative scenarios and §8.4's six walkthrough rows, with Guide §9 as supporting risk and check material. Every Goal capability area is reachable from both a scenario and a walkthrough path. Security and verification are cross-cutting rather than having dedicated rows, which §8.4 states as a design choice and which is defensible: treating security as a condition on every path is stronger than isolating it in one scenario, and multi-principal coverage is real through the two permitted and denied source groups required at line 944.

Every consequential scenario assertion was checked against the design line that must deliver it, and all are supported, including the post-backup cursor failing in the new recovery epoch, the absence of a current-geometry fallback, and a transformed payload not inheriting an unverified signature claim. No scenario asserts a guarantee the design does not establish, which is worth recording because the opposite is a common defect in scenario sets. The batch's substantive product is the four remedy locations recorded under F-03, F-19, F-18 and F-11 above. No research report was reopened. Evidence: [38-pass-3c-33-batch15-scenario-pass.md](evidence/38-pass-3c-33-batch15-scenario-pass.md).

**Verification-quality pass (iteration 34, queue batch 16):** independent expected answers, meaningful failure detection and appropriate real-system checks were assessed against Guide §§8.1 with 8.1.1, 8.3, 10 and 7.2. All three are strongly established. Independent expectations appear in three places, with line 901 controlling. Failure detection is the most developed part of the strategy: a test must be shown to detect the intended mistake, a setup or compilation failure is explicitly not that proof, and line 902 refuses test counts, coverage thresholds and mutation scores as establishing conformance. Real-system checks are governed by a rule about which layer may support which claim, closing the mock, sleep and paused-clock loopholes by name. Every Goal capability has a layer that can observe its claims, and no gap was found between what the strategy promises and what its layers deliver. The batch's substantive results are the F-12 relocation and the F-09 refinement recorded above. Evidence: [39-pass-3c-34-batch16-verification-quality.md](evidence/39-pass-3c-34-batch16-verification-quality.md).

**Complete leaf-to-issue comparison (iteration 35, queue batch 17):** all 286 issues were compared against their Roadmap leaves on nine dimensions, and no difference was found. The result rests on exact containment rather than a threshold: every leaf's `Scope` text and every leaf's `Done` text appears verbatim in its linked issue, 286 of 286 on both. Before accepting a zero, each of the nine checks was run against an injected difference and all nine fired, and a deliberately wrong leaf-to-issue pairing was correctly flagged. One reviewer error surfaced and was corrected during the run: an initial report of 286 title mismatches turned out to be a normalization defect that did not strip the bracketed leaf-ID prefix the issue titles carry.

The shared boilerplate was reviewed once and has exactly one variant of each shared section across all 286 issues. It carries the Guide's verification practices into every unit of work, requiring that checks actually ran and were not relabelled as success, that tests use independent expected results and demonstrate detection of wrong behavior, and that failures and unrun checks are stated honestly. The issues are a strict superset of their leaves, adding ten consistently labelled fields.

Two consequences are recorded. The queue's largest sizing uncertainty resolves favourably, because matching content genuinely need not be reviewed twice, so the batches that consume this inventory are lighter than allowed for rather than heavier. And the documented issue read count stays at **18 of 286**: the bodies were fetched and compared mechanically, which is not a reviewer reading them. Evidence: [41-pass-3c-35-batch17-leaf-to-issue-comparison.md](evidence/41-pass-3c-35-batch17-leaf-to-issue-comparison.md).

**Whole-backlog dependency, coverage and sizing analysis (iteration 36, queue batch 18):** the graph across 9 phases, 41 capability groups and 286 tasks is clean. It has 1,676 edges, exactly one root at the Roadmap's own starting task, a longest chain of 136, and no cycle, dangling reference, self-dependency, forward dependency or backward cross-phase dependency. Phase group and subtask counts reconcile to the phase table exactly. Phase prerequisites are consistent for all nine phases once compared against the transitive closure, which is the correct test because the phase table names proximate prerequisites rather than the full set.

Six of six graph checks were proven to detect injected defects, the closure check was probed with a control, and the parser that reads the Roadmap's dependency prose was validated on six shapes including a trailing explanatory clause that must not be read as further dependencies. Scope coverage is complete: all 37 cited Guide references are real sections, all 20 Guide capability sections are cited by some task, all six selected filtering classes are covered, and all nine Goal capability areas are represented.

Three apparent problems were inspected and all three were artifacts of the analysis: two filtering classes looked uncovered under literal string matching but are covered under short names, the phase prerequisite check first read the wrong table column, and comparing declared rather than transitive prerequisites was the wrong question. They are recorded because the discipline cuts both ways. A sensitive check that asks the wrong question produces confident nonsense, so every non-zero result was inspected before being reported, just as every zero was tested for sensitivity before being accepted. Evidence: [42-pass-3c-36-batch18-dependency-coverage-sizing.md](evidence/42-pass-3c-36-batch18-dependency-coverage-sizing.md).

**Phase 1 task assessment (iteration 37, queue batch 19):** the 24 tasks of issues #3 to #26 were assessed in five groups at capability-group boundaries, each as one unit. **All 24 are adequate.** The tasks are notably disciplined: every exclusion names the sibling that owns what it defers, several forbid a specific wrong shortcut rather than only stating the goal, and one documents an honest limitation, that internal locking cannot detect a stale client's lost update when no precondition was supplied, rather than overclaiming.

The shared version-pinning construction was assessed once and carried to all 286. Every issue cites an older Guide and Goal version than the current ones, which is sound rather than stale: the drift is disclosed and instructed by the issues themselves, over two thousand commit-pinned links mean each reference resolves to the text it was written against, the only links that track the current branch are the contributor guide where currency is wanted, and the single Guide change since touched anchors no issue cites.

The batch's material result is recorded under F-12 above. Two earlier observations were confirmed at task level: the restore task's acceptance does not mention audit, matching what the scenario pass found, and the identifier task covers the time-authority half of the UUIDv7 risk but not the privacy half. Evidence: [43-pass-3c-37-batch19-phase1-task-assessment.md](evidence/43-pass-3c-37-batch19-phase1-task-assessment.md).

**Phase 2a task assessment (iteration 38, queue batch 20):** the 31 tasks of issues #27 to #57 were assessed in three groups. **All 31 are adequate.** Three habits recur, each answering a concern this review holds. They refuse to invent rules the source does not contain, excluding an imposed finite-only numeric rule, forbidding alphabetical ordering guesses, and requiring explicit failure rather than inventing a default time zone. They construct discriminating rather than confirming cases, most clearly in requiring that a fixture selecting one alternative while supplying another's value must not pass merely because some alternative validates. And they tie to specific Guide lines, including contract immutability, the quality interpretation and its rule that an upstream schema pass is not proof, per-record quality references, label-based property identity, denying a representation rather than stripping fields, and routing Property Definitions to SensorML.

The read count was also placed on a sound basis. It is now derived from distinct issue IDs rather than from adding batch totals, which corrected 42 to 37 and then immediately caught a further overlap in this batch before it could be published. Evidence: [44-pass-3c-38-batch20-phase2-task-assessment.md](evidence/44-pass-3c-38-batch20-phase2-task-assessment.md).

**Phase 3 task assessment (iteration 40, queue batch 22):** the 39 tasks of issues #89 to #127 were assessed in five groups. **All 39 are adequate.** These tasks carry the Guide's hardest rules verbatim: a populated stream's schema cannot be changed by calling the change compatible or storing an internal revision; the cascade and conflict rules are reproduced exactly; the retry key stays optional and distinct measurements are never deduplicated by equal values; freshness is assessed without a universal readiness score; and ordinary observation arrival cannot silently create a System Event.

The `latest`-selection task deserves naming. It applies the selector after authorized scope and every other native predicate and **retains ties**, excluding ingestion order as freshness and any global selection before scope. That is the Guide's rule in full, and its fixtures seed exact-time ties, a newer excluded feature, a newer denied observation and late arrivals, so the discriminating cases are built in rather than assumed. One further task refuses to promote placeholder vocabulary into invented standard terms, which is the same discipline the Guide applies to relation names.

The coverage counts were also placed on a sound footing: they are now computed from the per-issue records, so the assessed, targeted-only and unique-read figures reconcile by construction rather than by arithmetic anyone has to repeat. Evidence: [46-pass-3c-40-batch22-phase3-task-assessment.md](evidence/46-pass-3c-40-batch22-phase3-task-assessment.md).

The remaining queue covers the per-task assessment of issues #128 to #288 in batches 23 to 26, and the final assessment.

## Important corrections preserved outside Copilot's last report

These are Codex review qualifications already communicated in the conversation. They do not silently overwrite the archived reviewer reports or count as the project lead accepting design changes.

- **F-12:** Existing independent-oracle provisions are substantive. A separate package or its dependency graph is not sufficient proof of oracle independence.
- **F-14:** Existing root-parent tests are not missing. Required UID-form write mapping, optional canonical-URL compatibility and optional returned UID metadata are separate choices.
- **F-18:** Do not turn a schema annotation into a complete ownership/derivation algorithm. Distinguish false/null and Command/Feasibility. No new lifecycle follows.
- **F-19:** Durable atomic records do not by themselves claim append-only protection or tamper evidence. Role separation and authorized retention need an explicit boundary if selected. The research's local hash/checkpoint recommendation was not conditional in every research profile, but that does not automatically make it a Guide requirement.
- **F-20:** Audit failure cannot authorize the denied action. This does not independently determine every HTTP failure response or require a separate failure spool. The proposed denial categories are not approved policy.
- **F-21:** Resource provenance can carry creator/context assertions without reproducing the source server's full authenticated audit trail. A backup preserves the recovery point, not all later history.
- **F-22:** Generation, release to an authorized file/sink, transport handoff and recipient receipt are different events. Actor/recipient/scope metadata can itself be protected. An export-audit requirement is a proposed strengthening, not proof of an already required implemented safeguard.
- **Offline authentication:** Existing JWTs can remain usable only within token, cached-trust and policy validity. Do not universally require a locally reachable identity provider for every such request; renewal and refresh are separate dependencies.
- **Offline command audit:** Transactional pre-dispatch records do not guarantee outcome recording after storage failure or rollback of physical effects. A local store does not make all network dependencies irrelevant.
- **Exchange integrity:** Retained exchange/revision/fingerprint evidence can reveal inconsistencies without a later resource change. Such checks are not source authentication and do not guarantee detection of consistently substituted unknown data.
- **Assurance:** Planned tests are not executed verification; one admitted dispatch is not exactly-once physical execution; refusing a TLS-1.3-only profile is not permission to use obsolete security settings.

## Continuing and using these findings

The current work cursor and the next bounded batch are maintained in [review-state.json](review-state.json). Follow [instructions.md](instructions.md); do not reconstruct the next task from historical reports or private memory.

Models may disagree with a finding. Record the evidence and the disagreement under that finding, continue the current batch, and settle its disposition during the relevant comparison or final consolidation. The Codex qualifications above are evidence-informed comments, not authority over an independent reviewer.

Keep review disposition separate from implementation: supported findings identify the affected issue and when a decision or fix is needed. Optional improvements remain optional; withdrawals remain visible. A completed review does not require implementing its recommendations.

## Source order

The archive filenames 01-15 are chronological: Pass 1, Pass 2, Pass 3a, Pass 3b, then Pass 3c iterations 1-11. Later reports can narrow or withdraw earlier claims. Original files are retained unchanged so a handoff does not erase those corrections or recreate rejected findings. File 16 is the superseded resumption brief (history only). From 17 onward, evidence files are reviewer-authored iteration reports saved directly to the repository during the iteration they describe; their manifest entries record the authored hash as both original and published.

Snapshot baselines reported by the review:
- Planning: [a310eaee2e80bb861197822a3c5bb12164ca9ac3](https://github.com/DGIWG-P507/glaux/tree/a310eaee2e80bb861197822a3c5bb12164ca9ac3).
- Server: [b1a80298305fd62164160058cde6d1794f533174](https://github.com/DGIWG-P507/glaux-server/tree/b1a80298305fd62164160058cde6d1794f533174).

Those are snapshot references, not claims that remote repositories can never change.
